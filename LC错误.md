### **为什么 `nums.length == 0` 要放在 `nums[start] != target` 前面？**

这个顺序非常重要，因为 **短路求值（Short-Circuit Evaluation）** 可以避免数组越界错误（`ArrayIndexOutOfBoundsException`）。
在 Java（以及大多数编程语言）中，逻辑运算符 `||`（OR）和 `&&`（AND）具有 **短路特性**：

- **`A || B`**：如果 `A` 为 `true`，就不会计算 `B`（因为整个表达式已经是 `true`）。
    
- **`A && B`**：如果 `A` 为 `false`，就不会计算 `B`（因为整个表达式已经是 `false`）。

如果顺序反过来：`nums[start]` 会先执行，但 `nums` 是空的 →`ArrayIndexOutOfBoundsException`

- **先检查边界条件（如 `nums.length == 0`）**，再访问数组元素。

### result.stream().mapToInt(Integer: :intValue ).toArray()
List< Integer> result = new ArrayList<>();
result.steam()：转换为Steam流，之后可以启用函数式编程
.mapToInt(Integer: : intValue)：将`Stream<Integer>`转换为`IntStream`
	 `mapToInt()`是一个中间操作，将流中的元素映射为int类型
	 `Integer::intValue`是方法引用，等价于lambda表达式`x -> x.intValue()`
.toArray()：将intSteam转换为int[]数组


### **为什么数组（`used[nums[i] + offset]`）比 `HashMap` 更节省内存且访问更快？**

#### **1. 内存节省原因**
| **因素**          | **数组**                          | **`HashMap`**                     |
|-------------------|----------------------------------|-----------------------------------|
| **数据结构**      | 连续内存的 `int[]`               | 哈希表（`Node<K,V>[]` + 链表/红黑树） |
| **存储开销**      | 仅需存储值（`int`）              | 需存储键、值、哈希、指针等额外字段   |
| **内存占用**      | 固定大小（如 `201` 个 `int`）    | 动态扩容，默认负载因子 0.75 额外占用 |
| **示例**          | `new int[201]` → 约 804 字节     | 即使只存 10 个元素，可能占用 1KB+   |

**关键点**：  
- 数组直接存储原始数据，而 `HashMap` 需要维护哈希桶、链表等结构，每个键值对占用更多内存（通常 3~4 倍于数组）。

---

#### **2. 访问速度更快原因**
| **因素**          | **数组**                          | **`HashMap`**                     |
|-------------------|----------------------------------|-----------------------------------|
| **访问步骤**      | 1. 计算偏移量 `num + offset`<br>2. 直接访问内存地址 | 1. 计算 `hash(key)`<br>2. 处理哈希冲突（链表/红黑树遍历） |
| **时间复杂度**    | 严格 `O(1)`（无冲突）            | 平均 `O(1)`，最坏 `O(n)`（哈希冲突时） |
| **CPU 缓存友好**  | 连续内存，预加载效率高            | 内存分散，缓存命中率低              |

**关键点**：  
- 数组通过 **直接寻址** 访问，而 `HashMap` 需要 **哈希计算+冲突处理**，多一步间接操作。
- 现代 CPU 对连续内存（数组）的缓存优化更高效。

---

#### **3. 性能对比示例**
##### **场景：统计 `-100` 到 `100` 的数字频率**
```java
// 方法1：数组 + 偏移
int[] used = new int[201]; // -100→0, 100→200
for (int num : nums) {
    used[num + 100]++;
}

// 方法2：HashMap
HashMap<Integer, Integer> map = new HashMap<>();
for (int num : nums) {
    map.put(num, map.getOrDefault(num, 0) + 1);
}
```
**性能差异**：
1. **内存**：`used` 数组固定占用 `201 * 4B = 804B`，而 `HashMap` 存储相同数据可能需 3KB+。
2. **速度**：数组访问比 `HashMap` 快 2~5 倍（实测差异）。

---

#### **4. 何时选择数组替代 `HashMap`？**
✅ **适用场景**：
- 键的范围已知且较小（如 `-100` 到 `100`）。
- 需要极致性能优化（如算法竞赛、高频调用代码）。

❌ **不适用场景**：
- 键范围过大（如 `±1e9`），会导致数组内存爆炸。
- 键类型非整数（如字符串、对象）。

---

#### **5. 底层原理深入**
##### **数组的快速访问**
```java
used[num + offset] = 1;
```
- **编译后**：直接转换为内存地址 `基地址 + (num + offset) * sizeof(int)`，单指令完成。

##### **`HashMap` 的访问开销**
```java
map.get(num);
```
- **步骤**：
  1. 计算 `hash(num)` → 可能涉及多次位运算。
  2. 定位桶位置 `index = hash % bucket_size`。
  3. 遍历链表或红黑树（冲突时）。

---

#### **6. 实测数据参考**
| 操作               | 数组 (ns/op) | `HashMap` (ns/op) |
|--------------------|-------------|-------------------|
| 插入/更新          | 5~10        | 20~50             |
| 查询               | 3~5         | 10~30             |

（数据基于 JMH 基准测试，JDK 17）

---

#### **总结**
- **更省内存**：数组无额外结构开销，`HashMap` 需存储元数据。
- **更快访问**：数组直接寻址，`HashMap` 需哈希计算和冲突处理。
- **适用条件**：**键范围小且已知**时优先用数组，否则用 `HashMap`。

**决策树**：  
```plaintext
if (键是整数 && 范围已知且较小) {
    用数组 + 偏移;
} else {
    用 HashMap;
}
```


### `int m = (left + right) >>> 1;` 是一种**高效计算中间值（mid）的方式**，通常用于二分查找（Binary Search）等算法中。作用是避免整数溢出，同时计算 `left` 和 `right` 的平均值（即中间位置）。

#### **1. 代码分解**
```java
int m = (left + right) >>> 1;
```
- **`left + right`**：计算两个数的和。
- **`>>> 1`**：将结果**无符号右移 1 位**（等价于除以 2，但更安全）。
- **`int m`**：最终结果赋值给 `m`（即中间值）。

---

#### **2. 为什么用 `>>>` 而不是 `/ 2`？**
| 方法 | 示例（`left=1`, `right=2147483647`） | 结果 | 问题 |
|------|--------------------------------------|------|------|
| `(left + right) / 2` | `(1 + 2147483647) / 2` | **溢出**（`-1073741824`） | `left + right` 可能超过 `Integer.MAX_VALUE` |
| `(left + right) >>> 1` | `(1 + 2147483647) >>> 1` | `1073741824`（正确） | 无符号右移避免溢出 |

##### **关键区别**：
- **`/ 2`**：如果 `left + right` 超过 `Integer.MAX_VALUE`（即 `2³¹ - 1`），会变成负数，导致计算错误。
- **`>>> 1`**：无符号右移直接对二进制位操作，高位补 0，避免溢出问题。

---

#### **3. 二进制示例**
假设 `left = 1`，`right = 3`：
1. **`left + right = 4`**（二进制 `0000...0100`）。
2. **`>>> 1`**：右移 1 位 → `0000...0010`（即 `2`，正确中间值）。

如果 `left = 2147483647`，`right = 2147483647`：
1. **`left + right = 4294967294`**（二进制 `1111...1110`，超出 `int` 范围，但位模式正确）。
2. **`>>> 1`**：右移 1 位 → `0111...1111`（即 `2147483647`，正确）。

---

#### **4. 对比其他写法**
| 写法 | 安全性 | 适用场景 |
|------|--------|----------|
| `(left + right) / 2` | ❌ 可能溢出 | 仅限 `left + right` 不会溢出时 |
| `left + (right - left) / 2` | ✅ 安全 | 通用写法，但稍慢 |
| `(left + right) >>> 1` | ✅ 安全且高效 | **推荐**（Java 标准库常用） |

---

#### **5. 为什么 Java 标准库用 `>>>`？**
在 `java.util.Arrays` 的二分查找实现中，明确使用了 `>>>`：
```java
// JDK 源码示例
private static int binarySearch(int[] a, int key) {
    int low = 0;
    int high = a.length - 1;
    while (low <= high) {
        int mid = (low + high) >>> 1; // 无符号右移
        if (a[mid] < key) low = mid + 1;
        else if (a[mid] > key) high = mid - 1;
        else return mid;
    }
    return -1;
}
```
**原因**：
- 性能优化：位运算比除法快。
- 安全性：避免潜在的整数溢出。

---

#### **6. 其他语言的类似操作**
| 语言 | 等效写法 | 说明 |
|------|----------|------|
| C/C++ | `mid = left + ((right - left) >> 1);` | 需确保 `right >= left` |
| Python | `mid = (left + right) // 2` | Python 整数无溢出 |

---

#### **总结**
- **`(left + right) >>> 1`** 是 Java 中计算中间值的**安全且高效**的方式。
- 核心优势：**避免整数溢出**，同时比 `left + (right - left) / 2` 更快。
- 适用场景：二分查找、分治算法等需要计算中间索引的情况。


### **1. `digits.charAt(num)`**

- `digits` 是一个字符串，比如 `"23"`。
    
- `charAt(num)` 返回 `digits` 的第 `num` 个字符（索引从 `0` 开始）。
    
    - 例如：
        
        - `digits = "23"`，`num = 0` → `digits.charAt(0)` 返回 `'2'`（字符 `'2'`，不是数字 `2`）。
            
        - `digits = "23"`，`num = 1` → `digits.charAt(1)` 返回 `'3'`。
            

### **2. `digits.charAt(num) - '0'`**

- 由于 `digits.charAt(num)` 返回的是 `char` 类型（如 `'2'`），而我们需要的是 **数字**（如 `2`），所以用 `- '0'` 进行转换：
    
    - `'2' - '0' = 2`（因为 `'2'` 的 ASCII 码是 `50`，`'0'` 是 `48`，`50 - 48 = 2`）。
        
    - `'3' - '0' = 3`（同理）。
        
- 这样，`digits.charAt(num) - '0'` 就把字符数字转换成了真正的 `int` 数字。
    

### **3. `numString[digits.charAt(num) - '0']`**

- `numString` 是一个 **字符串数组**，通常定义如下：
    
    java
    
    String[] numString = {
        "",     // 0（通常 0 和 1 没有字母）
        "",     // 1
        "abc",  // 2
        "def",  // 3
        "ghi",  // 4
        "jkl",  // 5
        "mno",  // 6
        "pqrs", // 7
        "tuv",  // 8
        "wxyz"  // 9
    };
    
- `digits.charAt(num) - '0'` 计算出的数字（如 `2`）作为索引，取出 `numString` 中对应的字母串：
    
    - 例如：
        
        - `digits = "23"`，`num = 0` → `digits.charAt(0) - '0' = 2` → `numString[2] = "abc"`。
            
        - `digits = "23"`，`num = 1` → `digits.charAt(1) - '0' = 3` → `numString[3] = "def"`。
            

### **4. 最终赋值给 `String str`**

- 最终，`str` 存储的是当前数字对应的字母字符串：
    
    - 例如：
        
        - `digits = "23"`，`num = 0` → `str = "abc"`。
            
        - `digits = "23"`，`num = 1` → `str = "def"`。



### **为什么 `used[i - 1] == false` 和 `used[i - 1] == true` 都能去重？**

在解决 **全排列 II（LeetCode 47）** 这类问题时，`used[i - 1] == false` 和 `used[i - 1] == true` 都可以实现去重，但它们的 **去重逻辑和适用场景不同**。以下是详细解析：

---

#### **1. 两种去重条件的区别**
##### **(1) `used[i - 1] == false`（树层去重）**
```java
if (i > 0 && nums[i] == nums[i - 1] && !used[i - 1]) {
    continue;
}
```
- **作用**：跳过同一树层（同一递归层级）的重复数字。
- **逻辑**：
  - 如果 `nums[i] == nums[i - 1]` 且 `nums[i - 1]` **未被使用**（`used[i - 1] == false`），说明当前数字 `nums[i]` 是同一树层的重复，直接跳过。
- **示例**：
  - `nums = [1, 1, 2]`，当第一个 `1` 未被使用时，跳过第二个 `1`，避免同一树层重复。

###### **1. 前提条件：数组已排序**
首先，代码中调用了 `Arrays.sort(nums)`，使得相同的数字会相邻排列。例如：
```java
nums = [1, 1, 2]  → 排序后仍为 [1, 1, 2]
```

---

###### **2. `used[i - 1] == false` 的含义**
- **`used[i - 1]`** 表示前一个相同的数字 `nums[i - 1]` 是否被使用过。
- **`used[i - 1] == false`** 说明 `nums[i - 1]` **在当前递归层级未被使用**（即已经被回溯撤销）。

---

###### **3. 为什么可以跳过？**
- **场景分析**：
  1. 当程序第一次遇到 `nums[i - 1]` 时，会尝试使用它（`used[i - 1] = true`），进入下一层递归。
  2. 当递归返回（回溯）时，`used[i - 1]` 会被重置为 `false`。
  3. 此时如果遇到 `nums[i] == nums[i - 1]` 且 `used[i - 1] == false`，说明：
     - `nums[i - 1]` 已经**在同一树层被尝试过**，并且所有包含它的可能性已经被穷举。
     - 当前 `nums[i]` 是**同一树层的重复数字**，继续使用会导致重复解。

- **类比**：
  - 假设你有一堆相同的硬币，第一次拿了一枚硬币 A，尝试了所有组合后放回。
  - 第二次再拿一枚相同的硬币 B，由于 A 和 B 完全一样，拿 B 的所有组合和拿 A 的组合**完全重复**，因此可以直接跳过。

---

###### **4. 具体例子分析**
以 `nums = [1, 1, 2]` 为例：
1. **第一层递归**：
   - 选择第一个 `1`（`nums[0]`），`used[0] = true`，进入下一层。
   - 回溯时，`used[0] = false`。
2. **再次第一层递归**：
   - 遇到第二个 `1`（`nums[1]`），此时 `nums[1] == nums[0]` 且 `used[0] == false`。
   - **跳过**：因为第一个 `1` 已经在本层尝试过所有可能性，第二个 `1` 的选择会生成完全相同的路径。

---

###### **5. 对比 `used[i - 1] == true` 的情况**
如果改为 `used[i - 1] == true`：
- 表示 `nums[i - 1]` 正在当前路径中使用，此时 `nums[i]` 是同一路径的重复数字。
- **允许使用**：例如 `[1, 1, 2]` 中，两个 `1` 可以在同一路径中被使用。

---

###### **6. 树层去重 vs. 树枝去重**
| **条件**               | **去重类型** | **效果**                          | 示例（`nums = [1, 1, 2]`）       |
|------------------------|--------------|-----------------------------------|----------------------------------|
| `!used[i - 1]`         | 树层去重     | 避免同一层级重复选择相同数字       | 跳过第二个 `1`，避免 `[1,1,2]` 重复生成 |
| `used[i - 1]`          | 树枝去重     | 避免同一路径重复选择相同数字       | 允许 `[1,1,2]`，但跳过其他重复   |

---

###### **7. 为什么树层去重更常用？**
- **避免完全重复的解**：树层去重能直接跳过相同数字的所有冗余分支。
- **效率更高**：减少不必要的递归调用。

---

###### **总结**
- **`nums[i] == nums[i - 1] && !used[i - 1]`** 的意思是：
  - **“如果当前数字和前一个数字相同，且前一个数字未被使用，说明当前数字是同一树层的重复，可以直接跳过”**。
- **本质**：通过排序和 `used` 数组，确保相同数字在同一层级只被选择一次。

**图示理解**：
```
树层去重（`!used[i - 1]`）：
第一层：
  选择第一个1 → 递归 → 回溯
  遇到第二个1时，发现第一个1未被使用（已回溯），说明是重复，跳过
```

##### **(2) `used[i - 1] == true`（树枝去重）**
```java
if (i > 0 && nums[i] == nums[i - 1] && used[i - 1]) {
    continue;
}
```
- **作用**：跳过同一树枝（同一路径）的重复数字。
- **逻辑**：
  - 如果 `nums[i] == nums[i - 1]` 且 `nums[i - 1]` **已被使用**（`used[i - 1] == true`），说明当前数字 `nums[i]` 是同一路径的重复，直接跳过。
- **示例**：
  - `nums = [1, 1, 2]`，当第一个 `1` 已被使用时，允许第二个 `1` 加入路径，生成 `[1, 1, 2]`。

---

#### **2. 为什么两种条件都能去重？**
虽然两种条件都能避免重复排列，但它们的 **去重阶段不同**：
| 条件                   | 去重阶段       | 效果                               | 适用场景                     |
|------------------------|----------------|------------------------------------|------------------------------|
| `used[i - 1] == false` | **树层去重**   | 避免同一层级选择重复数字            | 全排列、子集、组合等问题     |
| `used[i - 1] == true`  | **树枝去重**   | 避免同一路径选择重复数字            | 需要保留部分重复结果的问题   |

##### **本质原因**
- **排序后的数组**：`Arrays.sort(nums)` 确保相同数字相邻。
- **回溯的特性**：
  - 当 `used[i - 1] == false` 时，说明 `nums[i - 1]` 已经在该树层被尝试过并回溯撤销，因此 `nums[i]` 是重复的，应跳过。
  - 当 `used[i - 1] == true` 时，说明 `nums[i - 1]` 正在当前路径中使用，允许 `nums[i]` 继续使用（如 `[1, 1, 2]`）。

---

#### **3. 实例验证**
##### **输入**：`nums = [1, 1, 2]`
##### **(1) 使用 `!used[i - 1]`（树层去重）**
- **递归树**：
  ```
  第一层：选择第一个1（used[0]=true），跳过第二个1（因为 nums[1] == nums[0] 且 used[0]==false）
  第二层：选择第二个1（used[1]=true）
  第三层：选择2（used[2]=true）
  结果：[1, 1, 2]
  ```
- **输出**：`[[1, 1, 2], [1, 2, 1], [2, 1, 1]]`（无重复）

##### **(2) 使用 `used[i - 1]`（树枝去重）**
- **递归树**：
  ```
  第一层：选择第一个1（used[0]=true），允许第二个1（因为 used[0]==true）
  第二层：选择第二个1（used[1]=true）
  第三层：选择2（used[2]=true）
  结果：[1, 1, 2]
  ```
- **输出**：同样得到 `[[1, 1, 2], [1, 2, 1], [2, 1, 1]]`，但逻辑上是允许路径内重复。

---

#### **4. 如何选择正确的条件？**
- **需要严格去重**（如全排列、子集）：用 `!used[i - 1]`（树层去重）。
- **需要保留路径内重复**（如某些组合问题）：用 `used[i - 1]`（树枝去重）。

---

#### **5. 代码对比**
##### **(1) 树层去重（推荐）**
```java
if (i > 0 && nums[i] == nums[i - 1] && !used[i - 1]) {
    continue; // 跳过同一树层的重复
}
```
##### **(2) 树枝去重**
```java
if (i > 0 && nums[i] == nums[i - 1] && used[i - 1]) {
    continue; // 跳过同一路径的重复
}
```

---

#### **6. 总结**
- **`!used[i - 1]`**：用于 **树层去重**，避免同一递归层级选择重复数字。
- **`used[i - 1]`**：用于 **树枝去重**，避免同一路径选择重复数字。
- **效果**：两种条件在排序后的数组中都能去重，但树层去重更符合大多数场景需求。

**记忆技巧**：  
“树层去重看前一个是否未用（`false`），树枝去重看前一个是否已用（`true`）。”