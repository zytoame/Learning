`CREATE DATABASE database_name;` 用于创建数据库；`DROP DATABASE database_name;` 用于删除数据库；`SHOW DATABASES;` 用于显示所有数据库；`USE database_name;` 用于切换数据库。

说说用户和权限管理的命令
`CREATE USER 'username'@'host' IDENTIFIED BY 'password';` 用于创建用户；
`GRANT ALL PRIVILEGES ON database_name.table_name TO 'username'@'host';` 用于授予权限；
`REVOKE ALL PRIVILEGES ON database_name.table_name FROM 'username'@'host';` 用于撤销权限；
`DROP USER 'username'@'host';` 用于删除用户
### DLL操作表
CRUD
1. 创建表：create table 表名(列名，数据类型)
	eg：create table tb_user(id int, username varchar(20), password carchar(32));
2. 查看表：show tables；     desc 表名称；
3. 删除表：DROP TABLE 表名；  DROP TABLE IF EXISTS 表名；
4. 修改表
	1. 修改表名：ALTER TABLE 表名 RENAME TO 表名；
	2. 添加一列：ALTER TABLE 表名 ADD 列名 数据类型；
	3. 修改数据类型：ALTER TABLE 表名 MODIFY 列名 新数据类型；
	4. 修改列名和数据类型：ALTER TABLE 表名 CHANGE 列名 新列名 新数据类型
	5. 删除列：ALTER TABLE 表名 DROP 列名；
### DML行数据crud
1. 添加数据
	1. 给指定列：INSERT INTO 表名（列名1，列名2，···） VALUES（值1，值2，···）
	2. 批量添加：INSERT INTO 表名（列名1，列名2，···） VALUES（值1，值2，···），（值1，值2，···），（值1，值2，···）···；
2. 查询数据：SELECT column_names FROM table_name WHERE condition;
3. 修改表数据：UPDATE 表名 SET 列名1 = 值1 ，列名2 = 值2，WHERE 条件；不加条件则表中数据全部修改
4. 删除数据：DELDETE FROM 表名 WHERE 条件
### DQL
1. SELECT  字段列表
	FROM  表名列表
	WHERE  条件列表
	GROUP BY 分组列表
	HAVING  分组后条件
	ORDER BY  排序字段
	LIMIT  分页限定
2. 基础查询
	1. 查询列数据：SELECT 列名 FROM 表名
	2. 去除重复记录：SELECT DISTINCT 列名 FROM 表名
	3. 起别名：AS或者空格代替：列名 别名；列名 AS 别名
3. 条件查询：SELECT 字段列表 FROM 表名 WHERE 条件列表
	1. 模糊查询：like ‘条件’     _ 任意单字符，%任意多字符
		1. 查询名字中包含‘德’的学员信息：SELECT * FROM stu WHERE name LIKE ‘%德%’；
4. 分组查询：SELECT  字段列表  FROM  表名  WHERE 分组前条件限定  GROUP BY  分组字段名  HAVING 分组后条件过滤
    **`GROUP BY` 的作用**：告诉数据库"按哪些列分组计算"。
5. 排序查询：SELECT  字段列表  FROM  表名  ORDER BY  条件列表     升序：ASC；降序：DESC
6. 聚合函数：SELECT 聚合函数（列名） FROM 表名
	1. null不参与所有聚合函数计算
	2. 计数count，COUNT(column_name)统计非空值的数量，count（* ）统计所有行数
	3. 求平均：avg（列名）
7. 分页查询：SELECT  字段列表  FROM  表名  LIMIT  起始索引，查询条目数
	1. 起始索引：（当前页码-1） * 每页显示的条数
### DCL

连接两张表
	inner join
	left join
	cross join，笛卡尔连接，行数是相乘。交叉连接会返回两张表的笛卡尔积，也就是将左表的每一行与右表的每一行进行组合，返回的行数是两张表行数的乘积。

### 约束
1. 非空约束：NOT NULL
2. 唯一：UNIQUE
3. 主键：PRIMARY KEY。ALTER TABLE table_name ADD PRIMARY KEY (column_name);
4. 检查：CHECK （MySQL不支持）
5. 默认：DEFAULT
6. 外键：FOREIGN KEY。ALTER TABLE table_name ADD CONSTRAINT fk_name FOREIGN KEY (column_name) REFERENCES parent_table (parent_column_name);
7. 自动增长：auto_increment:当列是数字类型且唯一
### 多表查询
1. 子查询
	1. 单行单列：SELECT  字段列表  FROM  表名  WHERE  字段名 = （子查询）；%% 作为条件值，使用条件判断：=  ！=  >  <  %%
	2. 多行单列：SELECT  字段列表  FROM  表名  WHERE  字段名 in （子查询）；
	3. 多行多列：SELECT  字段列表  FROM  （子查询）  WHERE  条件；%%作为虚拟表%%
### 事务ACID
1. 开启：START TRANSACTION; 或者 BEGIN;
2. 提交事务：COMMIT
3. 回滚事务：ROLLBACK
4. 四大特征
	1. 原子性A：同时成功或者失败，如果事务执行过程中发生错误则回滚为事务开始前状态。（undo log）
	2. 一致性C：事务完成时，所有数据保持一致状态
	3. 隔离性I：多个事务之间，操作可见性。数据库允许多个并发操作对数据库进行读写操作，隔离性可以防止交叉执行时造成数据不一致。每个事务都有一个完整的数据空间，对其他并发事务是隔离的。（MVCC和锁机制）
	4. 持久性D：事务一旦提交或回滚，对数据库中的数据改变是永久的。（redo log）

### 数据库==三大范式==
关系型数据库设计中的核心原则，用于减少数据冗余、提高数据一致性。

#### **第一范式（1NF）**

- **定义**：确保每列的**原子性**（不可再分），即**每个字段都是最小数据单元**，无重复组或数组。
- **要求**：
    - 每列都是单一属性，不能是集合、数组或复合值。
    - 每行必须有唯一标识（如主键）。
- **示例**：  
  错误设计（非1NF）：`订单表(订单ID, 产品列表: [产品1, 产品2])`  
  正确设计（1NF）：`订单表(订单ID, 产品ID)`，产品信息单独存储。
#### **2. 第二范式（2NF）**
- **前提**：已满足1NF。
- **定义**：消除**部分函数依赖**，即非主键字段必须完全依赖于**整个主键**（而非主键的一部分）。
- **要求**：
  - 表必须有主键。
  - 非主键字段不能仅依赖主键的某一部分（针对联合主键的情况）。
  - **表中的每一列都和主键直接相关**
- **示例**：  
  错误设计（违反2NF）：`订单明细(订单ID, 产品ID, 产品名称)`（`产品名称`仅依赖`产品ID`，与`订单ID`无关）。  
  正确设计：拆分为`订单明细(订单ID, 产品ID)`和`产品表(产品ID, 产品名称)`。

---

#### **3. 第三范式（3NF）**
- **前提**：已满足2NF。
- **定义**：消除**传递函数依赖**，即非主键字段不能依赖于其他非主键字段。
- **要求**：
  - **非主键字段必须直接依赖于主键**，不能通过其他非主键字段间接依赖。
- **示例**：  
  错误设计（违反3NF）：`学生表(学号, 姓名, 学院, 院长)`（`院长`依赖`学院`，而`学院`依赖`学号`）。  
  正确设计：拆分为`学生表(学号, 姓名, 学院)`和`学院表(学院, 院长)`。

---

#### **总结**
| 范式 | 核心目标 | 关键操作 |
|------|---------|----------|
| 1NF  | 原子性  | 拆分复合字段 |
| 2NF  | 消除部分依赖 | 拆分部分依赖字段 |
| 3NF  | 消除传递依赖 | 拆分间接依赖字段 |

**注意**：  
- 范式是递进的（需先满足1NF，再满足2NF，依此类推）。  
- 实际设计中可能根据性能需求反范式化（如允许少量冗余）。  
- 更高范式（BCNF、4NF等）针对更复杂场景，但三大范式是基础。

### 建表时需要考虑哪些问题
1. 是否符合数据库的三大范式，确保字段不可再分，消除非主键依赖，确保字段仅依赖于主键
2. 选择合适的数据类型作为字段类型
3. 字符集尽量选择utf8mb4，支持中英文和表情符号等
4. 考虑分表（当数据量较大时）。水平分表来分散单表存储压力

### char和varchar的区别

char是固定长度的字符类型，不管实际存储的字符长度是多少，都只会占用10个字符空间char（10）。如果插入的数据小于1-字符，剩余的部分会用空格填充
varchar是可变长度的字符类型，实际上最大可以设置到65533（MySQL需要1到2个字节来表示字符串长度）

### blob和text有什么区别
blob 用于存储二进制数据，比如图片、音频、视频、文件等；但实际开发中，我们都会把这些文件存储到 OSS 或者文件服务器上，然后在数据库中存储文件的 URL。

text 用于存储文本数据，比如文章、评论、日志等。

### datetime和timestamp有什么区别

datetime直接存储日期和时间的完整值，与时区无关。默认值为null，占用八个字节，
timestamp存储Unix时间戳，受时区影响，秒数。默认值为当前时间，占四个字节。

### in和exists的区别

in：MySQL会首先执行子查询，然后将子查询的结果集用于外部查询的条件，意味着子查询的结果集需要全部加载到内存中。
	适用于子查询结果集较小的情况。如果返回大量数据，in的性能可能会下降，因为需要把整个结果集加载到内存
	**null值陷**：如果结果集中包含null值，可能导致以外的结果。例如，`WHERE column IN (subquery)`，如果 `subquery` 返回 `NULL`，则 `column IN (subquery)` 永远不会为真，除非 `column` 本身也为 `NULL`。
exists：对外部查询的每一行执行一次子查询，如果子查询返回任何行，则exists条件为真。关注的是子查询是否返回行，而不是返回具体的值
	适用于子查询结果集可能很大的情况。
	不受null值影响

### 记录货币用什么类型
如果是电商、交易、账单等涉及货币的场景，建议使用 DECIMAL 类型，因为 DECIMAL 类型是精确数值类型，不会出现浮点数计算误差。
如果是银行，涉及到支付的场景，建议使用 BIGINT 类型。可以将货币金额乘以一个固定因子，比如 100，表示以“分”为单位，然后存储为 `BIGINT`。这种方式既避免了浮点数问题，同时也提供了不错的性能。但在展示的时候需要除以相应的因子。

因为 FLOAT 和 DOUBLE 都是浮点数类型，会存在精度问题。
在许多编程语言中，`0.1 + 0.2` 的结果会是类似 `0.30000000000000004` 的值，而不是预期的 `0.3`。

### drop，DELETE，truncate
DROP 是物理删除，用来删除整张表，包括表结构，且不能回滚。

DELETE 支持行级删除，可以带 WHERE 条件，可以回滚。

TRUNCATE 用于清空表中的所有数据，但会保留表结构，不能回滚。

### UNION 与 UNION ALL 的区别？

UNION 会自动去除合并后结果集中的重复行。UNION ALL 不会去重，会将所有结果集合并起来。

## SQL查询语句的执行顺序

1. 先执行from确定主表，准备数据
2. ON 连接多个表的条件
3. 再执行join连接
4. 然后where进行过滤：减少数据量
5. group by 进行分组
6. having过滤聚合结果
7. SELECT选择最终列
8. distinct 进行去重
9. order by 排序
10. limit限制返回行数：减少数据传输

## MySQL第3-10条记录怎么查

用limit，SELECT * FROM tablename LIMIT 2， 8
2：偏移量，表示跳过前面两条记录了，从第三条开始
8：行数，表示从偏移量开始，返回8条记录

### MySQL函数

1. 处理字符串的函数
	1. 连接两个或多个字符串：CONCAT（）；
	2. 返回字符串长度：LENGTH（）
	3. 从字符串中提取子字符串：SUBSTRING（）
	4. 替换字符串中的某部分：REPLACE（）；SELECT REPLACE('沉默 王二', '王二', 'MySQL') AS replaced_string;
	5. 去除字符串两侧的空格或其他指定字符：TRIM（）
2. 处理数字的函数
	1. 返回绝对值：ABS（）
	2. 四舍五入到指定的小数位数：ROUND（）
	3. 返回除法操作的余数：MOD（）
3. 处理日期和时间
	1. 返回当前的日期和时间：NOW（）
	2. 返回当前的日期：CURDATE（）
4. 汇总函数
	1. SUM（）：计算数值列的总和
	2. AVG（）
	3. COUNT（）：计算行数
5. 逻辑函数
	1. IF（）
	2. CASE（）

### SQL的隐式数据类型转换

1. 整数+浮点数：整数被转换为浮点数：SELECT 1 + 1.0; -- 结果为 2.0
2. 字符串+整顿：字符串被转化为整数：SELECT '1' + 1; -- 结果为 2
3. 最好通过显示转换来规避：SELECT CAST('1' AS SIGNED INTEGER) + 1; -- 结果为 2

### SQL的语法树解析

SQL的语法树解析是将SQL查询语句转换为抽象语法树AST的过程，是数据库引擎处理查询的第一步，也是防止SQL注入的重要手段。
1. 词法解析：拆解SQL语句，识别关键字，表名。列名等
2. 语法分析：检查SQL是否符合语法规则，并构建抽象语法树
3. 语义分析：检查表，列是否存在，进行权限验证

### MySQL的基础架构

1. 连接层：管理客户端连接，验证用户身份，权限校验，连接管理。可以通过数据库连接池来提升连接的处理效率
2. 服务层：MySQL的核心，负责查询解析。优化、执行。SQL语句会经过解析、优化器优化、转发到存储引擎执行，返回结果。包含查询解析器，优化器，执行计划生成器、日志模块等
3. 存储引擎层：数据实际存储和提取。MySQL支持多种存储引擎。
### InnoDB是如何存储数据的？

按照数据页进行数据读写，按照行读写的话每次读写都是一次磁盘IO操作，没有效率。
数据库的最小IO操作的单位是页，数据页的默认大小是16kb

数据页的结构

数据页的use records是如何存储数据的？
**数据页中的数据按照“主键”顺序组成单向链表。其中数据页中有一个页目录起到检索的作用，提高搜索效率。**

**页目录由多个槽组成，每个槽指向每一个组的最后一个记录。这个组主键的最大值**。
**通过页目录查找时，通过二分查找定位槽，再在槽内遍历记录**

**InnoDB使用B+树索引**，非叶子节点存储都是页索引，只有叶子节点存储数据。节点内容是数据页。所有节点按照索引大小排序，构成**双向链表**，方便查询
如果叶子节点存储数据就是聚簇索引，一个表聚簇索引只能有一个；如果叶子节点存储主键值就是二次索引，可以有多个二次索引。
使用二次索引搜索时，如果在能查询到数据则是索引覆盖；如果不能查询到数据则根据主键值回表到聚簇索引查询数据行

### 执行一条 SQL 查询语句，期间发生了什么？
1. **连接器**：建立连接，管理连接、校验用户身份；
2. 查询缓存：查询语句如果命中查询缓存则直接返回，否则继续往下执行。MySQL 8.0 已删除该模块；
3. 解析 SQL，通过**解析器**对 SQL 查询语句进行词法分析、语法分析，然后构建语法树，方便后续模块读取表名、字段、语句类型；
4. 执行 SQL：执行 SQL 共有三个阶段：
	1. **预处理**阶段：检查表或字段是否存在；将 select * 中的 * 符号扩展为表上的所有列。
	2. **优化**阶段：基于查询成本的考虑， 选择查询成本最小的执行计划；负责确定 SQL 语句的执行计划，这包括选择使用哪些索引，以及决定表之间的连接顺序等。
	3. **执行**阶段：根据执行计划执行 SQL 查询语句，**执行器**调用存储引擎的 API 来进行数据的读写。从存储引擎读取记录，返回给客户端；存储引擎负责查询数据，并将执行结果返回给客户端。客户端接收到查询结果，完成这次查询请求。
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250816133108319.png)





联合索引的最左匹配原则，在遇到范围查询（如 >、<）的时候，就会停止匹配，也就是范围查询的字段可以用到联合索引，但是在范围查询字段的后面的字段无法用到联合索引。注意，对于 >=、<=、BETWEEN、like 前缀匹配的范围查询，并不会停止匹配，
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250816114122454.png)

### 索引的缺点：
需要占用物理空间，数量越大，占用空间越大； 
创建索引和维护索引要耗费时间，这种时间随着数据量的增加而增大； 
会降低表的增删改的效率，因为每次增删改索引，B+ 树为了维护索引有序性，都需要进行动态维护。


### update语句的执行过程

UPDATE t_user SET name = 'xiaolin' WHERE id = 1;5t
![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250819145252373.png)
1. 执行器负责执行，调用存储引擎的接口，通过主键索引树搜索**获取id=1的记录**
	1. id = 1这一行所在的数据页本来在buffer pool中就直接返回给执行器更新
	2. 记录不在buffer pool，将数据页从磁盘读入到buffer pool，返回记录给执行器
2. 执行器得到聚簇索引记录后会看一下更新前的记录和更新后的记录是否一样
	1. 一样：不进行后续更新流程
	2. 不一样：把更新前和后的记录都当中参数传给Innodb层，让Innodb真正的执行更新记录的操作
3. 开启事务，Innodb层更新记录前，首先**记录相应的undo log**，生成一条undo log，undo log会写入buffer pool中的undo页面。在内存修改该undo页面后需要记录对应的redo log。
4. Innodb层开始更新记录，先**更新内存（同时标记为脏页），将记录写到redo log里面**，更新完成。为了减少磁盘IO，不会立即将脏页写入磁盘，后续由后台线程选择一个合适的时机将脏页写入磁盘（WAL技术）。
5. 记录更新完成
6. 语句执行完成后，**记录该语句对应的binlog，保存到binlog cache**，在事务提交时才会统一将该事务运行过程中的所有binlog刷新到硬盘
7. 事务提交，两阶段提交
	1. **prepare阶段**：将redo log对应的事务状态设置为prepare，将redo log刷新到硬盘
	2. **commit阶段**：将binlog刷新到磁盘，调用引擎的提交事务接口，将redolog状态设置为commit，刷入到磁盘的redo log文件
8. 更新语句执行完成
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250918115429947.png)
### MySQL的段区页行

![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250918143617725.png)

### MySQL日志

#### undo log（回滚日志）：
	Innodb存储引擎层生成的日志，实现了事务中的原子性，主要用于事务回滚和MVCC（多版本并发控制）
在事务执行中途发生MySQL崩溃后，可以通过日志回滚到事务之前的数据
roll_pointer指针（将undo log串成一个链表，版本链）和trx_id事务id
通过Read View和undo log实现MVCC

- ·delete操作实际上不会立即直接删除,而是将delete对象打上delete flag,标记为删除,最终的删除操作是purge线程完成的。
- ·update分为两种情况：update的列是否是主键列。
	○ 如果不是主键列,在undo log中直接反向记录是如何update的。即update是直接进行的。
	○ 如果是主键列,update分两部执行:先删除该行,再插入一行目标行。

undo log和数据页的刷盘（持久化到磁盘）策略一样，通过redo log 保证持久化
bufferpool中有undo页，对undo页的修改都会记录到redolog。redo log每秒刷盘，提交事务也会刷盘，数据页和undo页都是靠这个机制保证持久化

#### buffer pool
	基于内存的

- 读取数据：
	- 存在于buffer pool中，客户端直接读取buffer pool中的数据，
	- 不存在，取磁盘读取
- 修改数据
	- 存在：直接修改buffer pool中数据所在的页，将其页设置为脏页（该页的内存数据和磁盘上的数据已经不一致），为了减少磁盘IO，不会立即将脏页写入磁盘，后续由后台线程选择一个合适时机将脏页写入磁盘
	- 合适时机：
		- redo log日志满了，主动触发脏页刷新
		- buffer pool空间不足需要将一部分数据页淘汰掉，如果淘汰的是脏页需要先同步到磁盘
		- MySQL认为空闲时
		- MySQL正常关闭前
		
- Innodb会为buffer pool申请一片连续的内存空间，按照默认的16kb大小划分出一个个的页，叫做缓存页。（每个缓存页都创建一个控制块，包括（缓存页的表空间，页号，缓存页地址，链表节点等等）控制块放在buffer pool最前面，接下来才是缓存页。控制块和缓存页之间为碎片空间
- MySQL刚启动时，使用的虚拟内存空间很大，而使用到的物理内存空间很小，是因为只有这些虚拟内存被访问后操作系统才会触发缺页中断，申请物理内存，接着将虚拟地址和物理地址建立映射关系。
- **开启事务后Innodb层更新记录前**，首先要记录相应的undo log ，如果是更新操作，就要生成一条undo log，写入buffer pool中的undo页面
-  查询一条记录时，Innodb会把整个页的数据加载到buffer pool中，再通过页里的【页目录】去定位到某条记录
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250819151313240.png)

- 管理空闲页
	- 将空闲缓存页的控制块作为链表（Free链表）的节点，
	- 链表头节点包含链表的头节点地址，尾节点地址，当前链表中节点数量
	- 需要从磁盘中加载一个页到buffer pool中，从链表取一个空闲缓存页，将对应的控制块信息填上，从链表移除
	
- 管理脏页
	- Flush链表，链表元素都是脏页，节点是控制块
- 管理脏页+干净页
	- LRU List
- 预读失效
	1. **预读**：系统像"贴心管家"，看你正在读A文件，就提前把B文件也准备好，觉得你马上要用。
	2. **失效**：结果你突然改主意去读C文件了，提前准备的B文件完全没用上，还占了桌子（缓存）的空间。
	- 解决：对LRU划分old区域和young区域
		- 预读的页插入old区域头部，淘汰old末尾的页
		- 访问时插入到young区域头部，young区域尾部的页被挤到old区域作为头部，不会有页被淘汰
		
- buffer pool污染
	- **没用的数据占了内存，有用的数据被迫“流落街头”**，导致数据库变慢。
	- 发生场景
		1. **大查询突击**：
		    - 比如执行 `SELECT * FROM huge_table`（全表扫描），瞬间塞满缓冲池。
		2. **冷数据霸占空间**：
		    - 这些数据只被访问一次，之后长期不用，但占着内存不释放。
		3. **淘汰机制不合理**：
		    - 类似LRU（最近最少使用）算法可能误判，把真正的热点数据挤出去。
	- 解决
		1. **分页查询**：
		    - 避免 `SELECT *`，改用 `LIMIT` 分批查。
		2. **预热缓冲池**：
		    - 高频查询手动加载到缓冲池（如MySQL的 `LOAD INDEX INTO CACHE`）。
		3. **调整淘汰策略**：
		    - 例如MySQL的 `innodb_old_blocks_time`，设置冷数据停留时间。
		    - 如果后续的访问时间与第一次访问的时间在某个时间间隔内，那么该缓存页就不会被从old区域移动到young区域的头部;
			- 如果后续的访问时间与第一次访问的时间不在某个时间间隔内，那么该缓存页移动到young区域的头部;
		4. **隔离缓冲池**：
		    - 为临时查询单独分配内存区域（如MySQL的临时表空间）。
	

#### redo log（重做日志）：Innodb存储引擎层生成的日志，实现了事务中的持久性，让MySQL有crash-safe能力，保证MySQL在任何时候崩溃重启后之前已提交的记录都不会丢失；将写操作从随机写变成顺序写，提升MySQL写入磁盘的性能。主要用于掉电等故障恢复

redo log 是**物理日志**,记录了某个数据页做了什么修改,比如对XXX 表空间中的YYY数据页ZZZ偏移量的地方做了AAA更新,每当执行一个事务就会产生这样的一条或者多条物理日志。

在事务提交时,只要先将redo log持久化到磁盘即可,可以不需要等到将缓存在Buffer Pool里的脏页数据持久化到磁盘。
当系统崩溃时,虽然脏页数据没有持久化,但是redo log已经持久化,接着MySQL重启后,可以根据redo log的内容,将所有数据恢复到最新的状态。

修改undo页面，需要记录对应的redo log，因为undo log 也要实现持久性保护

事务提交之前发生了崩溃，
	- 宕机崩溃：不需要通过undo log回滚，因为事务没有提交，事务的数据不会持久化，还是在内存中，宕机崩溃了数据就丢失了，事务没有提交成功，所以数据本身无意义，丢失了就丢失了
	- 不是宕机崩溃：事务执行错误，MySQL正常运行，重启后会通过undo log 回滚事务

事务提交后发生宕机崩溃，重启后通过redo log恢复事务
![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250819153043507.png)


当有记录需要更新时，Innodb引擎先更新内存并标记为脏页（该页的内存数据和磁盘上的数据已经不一致），为了减少磁盘IO，不会立即将脏页写入磁盘，后续由后台线程选择一个合适时机将脏页写入磁盘。WAL（write-ahead logging）技术：MySQL的写操作并不是立刻写道磁盘上，而是先写日志，再在合适时机写到磁盘上
- 合适时机：
	- redo log日志满了，主动触发脏页刷新
	- buffer pool空间不足需要将一部分数据页淘汰掉，如果淘汰的是脏页需要先同步到磁盘
	- MySQL认为空闲时
	- MySQL正常关闭前
![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250819152609995.png)

- redo log要写到磁盘，数据也要写磁盘
	- 写入redo log 的方式使用了追加操作，所以磁盘操作是**顺序写**
	- 写入数据需要先找到写入位置，才写到磁盘，所以磁盘操作是随机写
	- 顺序写比随机写高效，因此redo log 写入磁盘的开销更小
	
- 产生的redo log不是直接写到磁盘的，因为这样会产生大量的IO操作，而且磁盘的运行速度远慢于内存。所以没产生一条redo log时会先写入redo log buffer ，后续在持久化到磁盘
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250820175814016.png)
	
- redo log刷盘时机
	- MySQL正常关闭
	- 当redo log buffer中记录的写入量大于buffer内存空间的一半使会触发落盘
	- Innodb的后台线程每隔1秒，将redo log buffer持久到磁盘
	- 每次事务提交时都将缓存在redo log buffer里的redo log直接持久化到磁盘（可由Innodb_flush_log_at_trx_commit参数控制
		- 参数0：把缓存在redo log buffer中的redo log，通过调用write（）写到操作系统的page cache，然后调用fsync（）持久化到磁盘。
			- 所以MySQL进程的崩溃会导致上一秒钟所有事务数据的丢失
		- 参数2：调用fsync（）将缓存在操作系统中的page cache里的redo log持久化到磁盘。
			- 所以MySQL进程的崩溃不会导致上一秒钟所有事务数据的丢失，只有在操作系统崩溃或者系统断电的情况下，才可能丢失
		- 数据安全性：1 > 2 > 0
		- 写入性能：0 > 2 > 1
		- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250820182230388.png)
		
- redo log Group：重做日志文件组（ib_logfile0, ib_logfile1）
	- 每个redo log File大小固定一致
	- 以循环写的方式工作，先写0文件，写满切换1文件，再切换0文件
	- write pos：redo log当前记录写到的位置
	- checkpoint：当前要擦除的位置。一次checkpoint的过程就是脏页刷新到磁盘中变成干净页，标记redo log哪些记录可以被覆盖的过程
	- 红色部分：用来记录新的更新操作
	- 蓝色部分：待落盘的脏数据页记录
	- 如果write pos 追上了checkpoint：redo log文件满了，MySQL不能再执行新的更新操作，会被阻塞，会停下来将buffer pool中的脏页刷新到磁盘中，标记redo log哪些记录可以被擦除，对旧的redo log记录进行擦除，腾出空间，checkpoint顺时针移动，MySQL恢复正常运行，继续执行新的更新操作
	- 针对并发量很大的系统，设置redo log 的文件大小非常重要
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250820213822810.png)
	

#### binlog（归档日志）：server层生成的日志，主要用于数据备份和主从复制

binlog记录所有数据库表结构变更和表数据修改的日志，不会记录查询类的操作（select，show）

|      | binlog                                                                                                                                                                                                                                                              | redo log                                               |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 适用对象 | MySQL的**server层**实现的日志，所有存储引擎都可以使用                                                                                                                                                                                                                                  | Innodb存储引擎实现的日志                                        |
| 文件格式 | 1. STATEMENT（默认格式）：记录每一条修改数据的sql，主从复制中slave端再根据SQL语句重现。<br>**有动态函数问题**，如果用了uuid或者now等函数，在主库上的执行结果并不是在从库执行的结果，会导致复制的数据不一致<br>2. ROW：记录行数据最终被修改成什么样（不能称为逻辑日志），不会出现动态函数问题<br>缺点：**每行数据的变化结果都会被记录**，使binlog过大<br>3. MIXED：包含STATEMENT和ROW模式，根据不同情况自动使用ROW模式和STATEMENT模式 | 物理日志，记录的是某个数据页做了什么修改，比如对XXX表空间中的YYY数据页ZZZ偏移量的地方做了AAA更新 |
| 写入方式 | **追加写**，写满一个文件就创建一个新文件继续写，**不会覆盖以前的日志**，保存到是全量的日志                                                                                                                                                                                                                   | **循环写**，日志大小空间是固定的，全部写满就从头开始，保存未被刷入磁盘的脏页日志             |
| 用途   | 用于备份恢复，主从复制                                                                                                                                                                                                                                                         | 用于掉电等故障恢复                                              |

- 刷盘时间
	- 一个事务的binlog不能被拆开，要保证**一次性写入**。如果被拆开在备库执行就会被当做多个事务分段自行，破坏了原子性
	- 每个线程都有内存binlog cache
	- write：把日志写入到binlog文件，但没有持久化到磁盘，因为数据还缓存在文件系统的page cache里，write写入速度快，因为不涉及磁盘IO
	- fsync：将数据持久化到磁盘，涉及磁盘IO，所以频繁的fsync会导致磁盘的IO升高
	- sync_binlog：控制数据库的binlog刷到磁盘的频率
		- 0（默认）：每次提交事务只write，不fsync，后续交由操作系统决定何时将数据持久化到磁盘。风险最大，一旦主机发生异常重启，还没持久化到磁盘的数据就会丢失
		- 1：每次提交事务都会write，马上执行fsync。安全性最好。性能损耗最大。主机异常重启最多丢失一个事务的binlog。
		- N（N>1）：每次提交事务都write，但累积N个事务后才fsync。提高写入性能可以设置
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250820224230499.png)


#### 主从复制的实现

- **主从复制依赖于binlog**，记录MySQL上的所有变化并以**二进制形式**保存在磁盘中。复制的过程就是将binlog中的数据从主库传输到从库上
- **异步**：主库上执行事务操作的线程不会等待复制binlog的线程同步完成
- **写数据只写主库，读数据只读从库**。即使写请求会锁表或者锁记录，也不会影响读请求的执行。
1. **写入binlog**：主库写binlog日志，提交事务，更新本地存储数据
2. **同步binlog**：把binlog复制到所有从库上，每个从库把binlog写到暂存日志中。
	1. 从库会创建一个专门的IO线程，
	2. 连接主库的log dump线程，来接受主库的binlog日志，
	3. 再把binlog信息写入relay log 的**中继日志**里，
	4. 在返回给主库“复制成功”的响应
3. **回放binlog**：回放binlog并更新存储引擎中的数据。
	1. 从库创建一个用于回放binlog的线程，
	2. 去读relay log的中继日志，
	3. 回放binlog，**更新存储引擎中的 数据**，最终实现主从的数据一致性
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250820222748035.png)

- 1主2从1备主：从库数量不是越多越好。从库数量增加，从库连接上来的IO线程较多，主库也要创建同样多的log dump 线程来处理复制的请求，对主库资源消耗比较高，受限于主库的网络带宽
	
- 主从复制模型
	- 同步复制：主库提交事务的线程要等待所有从库的复制成功响应才会返回客户端结果。实际项目中少用：1. 性能差，因为要复制到所有节点才返回响应；2. 可用性差，主从库任一个数据库出问题都会影响业务
	- 异步复制（默认）：主库提交事务的线程并不会等待binlog同步到各从库，就返回客户端结果。一旦主库宕机，数据就会发生丢失、
	- **半同步复制**：5.7版本之后增加的，介于两者之间，事务线程不用等待所有的从库复制成功响应，只要任意一个从库复制成功响应回来就行。主库出现宕机至少还有一个从库有最新的数据，不存在数据丢失的风险
	
- 
#### 两阶段提交

事务提交后，redo log和binlog都要持久化到磁盘，但是这两个是独立的逻辑，可能出现半成功的状态，导致两份日志之间的逻辑不一致

- 两阶段提交的原因
	1. redo log刷入磁盘后MySQL宕机，binlog未写入：重启后通过redo log能将buffer pool中的字段恢复到新值，但是binlog没有这条更新语句，从库的字段是旧值，与主库的值不一致
	2. binlog刷入磁盘后MySQL宕机，redo log未写入：崩溃恢复后事务无效，字段还是旧值，而binlog记录了更新语句，主从架构中从库执行这条更新语句，字段是新值，与主库的值不一致

- 两阶段提交，每阶段都有协调者和参与者共同完成
	- 准备阶段（Prepare ）：协调者依次向两位参与者询问是否做好准备，参与者作出应答
	- 提交阶段（Commit）：如果参与者都准备好了，协调者宣布开始提交；如果有参与者回答没有准备好，协调者会对事物进行回滚操作
	
- 提交的过程
	- 使用内部XA事务。binlog是协调者，存储引擎是参与者
	- 客户端执行commit语句或者自动提交的情况下，MySQL内部开启一个XA事务，分两阶段完成XA事务的提交
		1. Prepare：将XID（内部XA事务的ID）写入redo log，同时将redo log 对应的事务状态设置为prepare，将redolog 持久化到磁盘
		2. Commit：把XID写入到binlog，将binlog持久化到磁盘，调用引擎的提交事务接口，将redolog状态设置为commit，此时该状态不需要持久化到磁盘，只需要write到文件系统的page cache中，因为**只要binlog写磁盘成功，就算redo log的状态还是prepare也没有关系，一样会被认为事务已经执行成功**。
		![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250820232555016.png)
	
- 两阶段提交性能差
	- 磁盘IO次数高：每个事务提交都会进行两次fsync（刷盘）
	- 锁竞争激烈：多事务情况下，需要加一个锁来保证提交的原子性，保证两个日志的提交顺序一致
	
- binlog组提交（group commit）机制，减少磁盘IO。
	MySQL5.6没有redo log 组提交，5.7将redo的刷盘延迟到flush阶段中，sync阶段前，为redolog做一次组写入。
	- 当多个事务提交的事务将多个binlog刷盘操作合并一个。prepare阶段不变，将commit阶段拆分为三个过程（每个阶段都有一个**队列**，有锁保护（只针对每个队列，不锁住提交事务的整个过程），保证事务写入的顺序）**锁粒度减小，使多个阶段可以并发执行，从而提升效率**
	1. **flush阶段**：多个事务按进入的顺序将binlog从cache写入文件（不刷盘）。作用：**支撑redo log的组提交**
		1. 第一个事务成为flush阶段的leader，后面到来的都是follower
		2. 获取队列中的事务组，由leader对redo log 做一次write + fsync，即**一次将同组事务的redo log刷盘**。完成prepare阶段。
		3. 将这组事务执行过程中产生的**binlog写入binlog文件**（调用write，不会调用fsync，不会刷盘，**binlog缓存在操作系统的文件系统中**）。参数都默认为1.
		- 如果完成后数据库崩溃，由于binlog中没有该组事务的记录，所以MySQL会在重启后回滚该组数据
	2. **sync阶段**：对binlog文件做fsync操作（多个事务的binlog合并一次刷盘）。作用：**支持binlog的组提交**
		1. 一组事务的binlog写入到binlog文件后，等待一段时间，组合更多事务的binlog，再一起刷盘。事务数量提交达到设置的值就马上刷盘
		- 如果数据库崩溃，由于binlog已经有了事务记录，MySQL会在重启后通过redolog刷盘的数据继续进行事务提交
	3. **commit阶段**：按顺序做Innodb commit操作。作用：承接sync阶段的事务，完成最后的引擎提交，使得sync可以尽早处理下一组事务，最大化组提交的效率。
		1. 调用引擎的提交事务接口，将redo log状态设置为commit

#### MySQL中的redo log（重做日志）和binlog（二进制日志）是两种非常重要的日志，它们在刷盘（写入磁盘）操作和时机上有所不同，但都为了保证数据的一致性和持久性。

##### Redo Log (InnoDB存储引擎)

*   **作用：** 主要用于保证事务的持久性（Durability），记录了对数据页的物理修改。即使数据库崩溃，也能通过redo log恢复到崩溃前的状态。
*   **刷盘操作：**
    1.  **写入redo log buffer：** 事务提交时，其产生的redo log首先写入内存中的redo log buffer。
    2.  **写入redo log file：** redo log buffer中的内容会周期性地（或在特定条件下）写入到磁盘上的redo log文件（`ib_logfileN`）。
    3.  **fsync到磁盘：** 最终，操作系统会执行`fsync`操作，将redo log文件中的数据真正同步到磁盘上。
*   **刷盘时机（由`innodb_flush_log_at_trx_commit`参数控制）：**
    *   **`innodb_flush_log_at_trx_commit = 0`：**
        *   每秒将redo log buffer中的数据写入redo log文件，并调用`fsync`将文件内容刷新到磁盘。
        *   事务提交时，只将redo log写入redo log buffer，不立即刷新到磁盘。
        *   **优点：** 性能最好。
        *   **缺点：** 如果MySQL进程崩溃，可能会丢失1秒内的数据；如果操作系统崩溃，可能会丢失更多数据。
    *   **`innodb_flush_log_at_trx_commit = 1`（默认值）：**
        *   每次事务提交时，都将redo log buffer中的数据写入redo log文件，并立即调用`fsync`将文件内容刷新到磁盘。
        *   **优点：** 数据最安全，完全符合ACID的持久性要求。
        *   **缺点：** 性能开销最大，因为每次提交都需要进行磁盘I/O。
    *   **`innodb_flush_log_at_trx_commit = 2`：**
        *   每次事务提交时，将redo log buffer中的数据写入redo log文件，但只将文件内容刷新到操作系统的文件系统缓存（page cache），不立即调用`fsync`到磁盘。
        *   操作系统会每秒将文件系统缓存中的数据刷新到磁盘。
        *   **优点：** 性能介于0和1之间。
        *   **缺点：** 如果MySQL进程崩溃，数据不会丢失；但如果操作系统崩溃，可能会丢失最后1秒内的数据。

##### Binlog (MySQL Server层)

*   **作用：** 主要用于数据复制（主从同步）、数据恢复（Point-in-Time Recovery）和审计。它记录了所有对数据库的逻辑修改（例如，SQL语句或行级别的变更）。
*   **刷盘操作：**
    1.  **写入binlog cache：** 事务执行过程中，其产生的binlog事件首先写入内存中的binlog cache。
    2.  **写入binlog file：** 事务提交时，binlog cache中的内容会写入到磁盘上的binlog文件（`mysql-bin.XXXXXX`）。
    3.  **fsync到磁盘：** 最终，操作系统会执行`fsync`操作，将binlog文件中的数据真正同步到磁盘上。
*   **刷盘时机（由`sync_binlog`参数控制）：**
    *   **`sync_binlog = 0`：**
        *   MySQL不控制binlog的刷新，由操作系统决定何时将binlog文件从文件系统缓存刷新到磁盘。
        *   **优点：** 性能最好。
        *   **缺点：** 如果操作系统或服务器崩溃，可能会丢失部分binlog数据。
    *   **`sync_binlog = 1`（MySQL 5.7.7+的默认值）：**
        *   每次事务提交时，都将binlog文件刷新到磁盘。
        *   **优点：** 数据最安全，保证binlog的完整性。
        *   **缺点：** 性能开销最大，因为每次提交都需要进行磁盘I/O。
    *   **`sync_binlog = N` (N > 1)：**
        *   每N次事务提交时，将binlog文件刷新到磁盘。
        *   **优点：** 在性能和数据安全性之间取得平衡。
        *   **缺点：** 如果在N次提交之间发生崩溃，可能会丢失N-1次事务的binlog数据。

##### 两阶段提交（Two-Phase Commit）

为了保证redo log和binlog之间的一致性（即一个事务要么都在redo log和binlog中，要么都不在），MySQL使用了内部的**两阶段提交**机制：

1.  **Prepare阶段：**
    *   InnoDB事务将redo log写入redo log文件（但可能未`fsync`到磁盘，取决于`innodb_flush_log_at_trx_commit`）。
    *   事务状态变为`PREPARED`。
2.  **Commit阶段：**
    *   MySQL Server将binlog写入binlog文件（并根据`sync_binlog`参数决定是否`fsync`到磁盘）。
    *   如果binlog写入成功，MySQL Server会通知InnoDB提交事务，此时InnoDB会完成redo log的`fsync`操作（如果`innodb_flush_log_at_trx_commit=1`）。
    *   事务状态变为`COMMITTED`。

这个机制确保了即使在崩溃恢复时，redo log和binlog也能保持一致，从而保证数据不丢失或不损坏。
### 在两阶段提交的不同时刻，异常重启会出现什么现象？
MySQL的两阶段提交（Two-Phase Commit, 2PC）机制是为了确保InnoDB存储引擎的redo log和MySQL Server层的binlog之间的数据一致性。这意味着**一个事务要么同时在redo log和binlog中都完成提交，要么都回滚**，从而保证数据不会丢失或损坏，并且主从复制也能正常进行。**两阶段提交是以binlog写成功为事务提交成功的标识**

在两阶段提交的不同时刻发生异常重启（崩溃），MySQL的恢复行为会有所不同：

- 核心原理：XA事务的协调者角色
	在MySQL内部，InnoDB存储引擎可以看作是一个资源管理器，而MySQL Server层则扮演了XA事务（eXtended Architecture Transaction）的协调者角色。协调者会记录事务的XID（Transaction ID），并用它来关联redo log和binlog。

#### 崩溃恢复时的现象

我们假设`innodb_flush_log_at_trx_commit = 1`和`sync_binlog = 1`，这是最安全（但性能开销最大）的配置，可以最大程度地保证数据不丢失。

##### 1. 崩溃发生在“准备阶段”之前（事务尚未开始或刚开始）

*   **状态：** 事务尚未开始写入redo log，或者redo log还在内存中的redo log buffer中，但尚未写入磁盘。binlog也未开始写入。
*   **现象：** MySQL重启后，由于没有任何持久化的事务信息，该事务会被完全丢弃。
*   **结果：** 数据保持一致，就像这个事务从未发生过一样。

##### 2. 崩溃发生在“准备阶段”之后，但“提交阶段”之前（即redo log已写入并处于PREPARED状态，但binlog尚未写入）

*   **状态：**
    *   InnoDB的redo log已经写入磁盘（或至少已刷新到操作系统的文件系统缓存，并被**标记为`PREPARED`状态**）。
    *   但MySQL Server尚未开始写入binlog。
*   **现象：**
    *   MySQL重启后，InnoDB会进行崩溃恢复。
    *   InnoDB扫描redo log，发现一个处于`PREPARED`状态的事务（通过XID识别）。
    *   InnoDB会去检查binlog中是否存在这个XID对应的事务。
    *   **关键：** 由于binlog中**没有这个XID的记录**（因为崩溃发生在binlog写入之前），InnoDB会判断该事务未完成，因此会**回滚（rollback）这个事务**。
*   **结果：** 数据保持一致。虽然redo log中记录了修改，但由于binlog中没有，事务被回滚，数据恢复到事务开始前的状态。

##### 3. 崩溃发生在“提交阶段”中（binlog已写入，但redo log尚未最终提交或fsync）

*   **状态：**
    *   InnoDB的redo log已处于`PREPARED`状态。
    *   MySQL Server已将binlog写入磁盘（假设`sync_binlog=1`，已`fsync`）。
    *   但MySQL Server尚未通知InnoDB完成最终的提交（即redo log尚未被标记为`COMMITTED`，或在`innodb_flush_log_at_trx_commit=1`的情况下，redo log的最终`fsync`尚未完成）。
*   **现象：**
    *   MySQL重启后，InnoDB进行崩溃恢复。
    *   InnoDB扫描redo log，发现一个处于`PREPARED`状态的事务（通过XID识别）。
    *   InnoDB会去检查binlog中是否存在这个XID对应的事务。
    *   **关键：** 由于binlog中已经**存在这个XID的记录**，InnoDB会判断该事务已经完成，因此会**提交（commit**）这个事务。它会完成redo log的最终写入和`fsync`，并将事务标记为`COMMITTED`。
*   **结果：** 数据保持一致。事务被成功提交，数据修改被持久化。这保证了binlog和数据文件的一致性，即使在崩溃后也能通过binlog进行恢复或主从同步。
	
##### 4. 崩溃发生在“提交阶段”之后（事务已完全提交）

*   **状态：**
    *   InnoDB的redo log已完成提交并`fsync`到磁盘。
    *   MySQL Server的binlog已写入并`fsync`到磁盘。
*   **现象：** MySQL重启后，InnoDB在恢复时会发现该事务已经处于`COMMITTED`状态，无需额外操作。
*   **结果：** 数据保持一致，事务已完全持久化。

#### 总结与参数影响

*   **`innodb_flush_log_at_trx_commit`的影响：**
    *   如果设置为0或2，redo log可能在崩溃时还在操作系统的文件系统缓存中，未真正写入磁盘。在这种情况下，即使事务进入了`PREPARED`状态，如果操作系统崩溃，redo log可能丢失，导致数据丢失。
*   **`sync_binlog`的影响：**
    *   如果设置为0或N（N>1），binlog可能在崩溃时还在操作系统的文件系统缓存中，未真正写入磁盘。在这种情况下，如果MySQL Server崩溃，binlog可能丢失。
    *   如果binlog丢失，而redo log已经处于`PREPARED`状态，那么InnoDB在恢复时会因为找不到对应的binlog记录而回滚事务。这可能导致主从不一致（主库回滚了，但如果从库已经收到了这个binlog事件并应用了，就会出现不一致）。
	
**两阶段提交的核心思想是：** **以binlog是否写入并持久化作为事务最终提交的标志**。如果binlog存在，即使redo log尚未完全提交，也会在恢复时提交；如果binlog不存在，即使redo log已处于`PREPARED`状态，也会在恢复时回滚。这确保了数据文件和binlog之间的一致性，对于数据恢复和主从复制至关重要。
	
	


### 优化MySQL磁盘I/O
MySQL磁盘I/O高是数据库性能瓶颈的常见原因之一。优化磁盘I/O通常需要从多个层面入手，包括硬件、操作系统、MySQL配置、数据库设计和查询优化。

#### 一、 硬件和操作系统层面优化

1.  **使用高性能存储设备：**
    *   **SSD/NVMe：** 相比传统HDD，SSD（固态硬盘）和NVMe SSD具有更高的IOPS（每秒读写操作数）和更低的延迟，是提升I/O性能最直接有效的方式。
    *   **RAID配置：**
        *   **RAID 10 (1+0)：** 兼顾性能和数据冗余，是数据库服务器的推荐选择。
        *   **RAID 0：** 性能最高但无冗余，不推荐用于生产环境。
        *   **RAID 5/6：** 写入性能较差，不适合写密集型数据库。
2.  **优化文件系统和挂载选项：**
    *   **文件系统选择：** Linux下推荐使用`XFS`或`Ext4`。
    *   **挂载选项：**
        *   `noatime`：禁用文件访问时间的更新，减少不必要的写入。
        *   `barrier=0` (Ext4) 或 `nobarrier` (XFS)：在某些情况下可以提升性能，但会增加数据丢失的风险（如果硬件有电池备份的缓存则风险较低）。
        *   `data=writeback` (Ext4)：数据写入模式，性能较好，但可能在崩溃时丢失少量数据。
3.  **调整操作系统I/O调度器：**
    *   对于SSD，推荐使用`noop`或`deadline`调度器，它们更适合随机I/O。
    *   对于HDD，`cfq`或`deadline`可能更合适。
    *   查看和修改：`cat /sys/block/sdX/queue/scheduler` 和 `echo deadline > /sys/block/sdX/queue/scheduler`。
4.  **优化操作系统缓存和内存管理：**
    *   **`vm.swappiness`：** 降低该值（例如设置为10或0），减少系统将活跃内存页交换到磁盘的频率，避免不必要的I/O。
    *   **`dirty_ratio` 和 `dirty_background_ratio`：** 控制脏页（内存中已修改但未写入磁盘的数据）占总内存的比例。适当调整可以平衡写入性能和数据安全性。

#### 二、 MySQL配置优化

主要针对InnoDB存储引擎，因为它是MySQL最常用的事务型存储引擎。

1.  **`innodb_buffer_pool_size` (最重要)：**
    *   **作用：** InnoDB缓存数据和索引的内存区域。
    *   **优化：** 设置为服务器物理内存的50%-80%（如果服务器只运行MySQL）。越大越好，可以缓存更多热点数据，减少磁盘I/O。
    *   **现象：** 如果该值过小，`Innodb_buffer_pool_reads`（从磁盘读取的页数）会很高，而`Innodb_buffer_pool_read_requests`（逻辑读请求数）与`Innodb_buffer_pool_reads`的比值（命中率）会很低。
2.  **`innodb_log_file_size` 和 `innodb_log_files_in_group`：**
    *   **作用：** 控制redo log文件的大小和数量。
    *   **优化：** 适当增大`innodb_log_file_size`（例如，从默认的48MB增大到几百MB甚至GB级别），可以减少redo log的切换频率和checkpoint的发生频率，从而减少脏页刷新到磁盘的I/O。
    *   **注意：** 过大可能导致崩溃恢复时间变长。
3.  **`innodb_flush_log_at_trx_commit`：**
    *   **作用：** 控制redo log刷盘策略。
    *   **优化：**
        *   `1` (默认)：每次事务提交都将redo log同步到磁盘，最安全，但I/O最高。
        *   `2`：每次事务提交将redo log写入文件系统缓存，每秒同步到磁盘。性能提升，但操作系统崩溃可能丢失1秒数据。
        *   `0`：每秒将redo log写入文件系统缓存并同步到磁盘。性能最好，但MySQL进程崩溃或操作系统崩溃都可能丢失数据。
    *   **选择：** 根据对数据安全性的要求和I/O瓶颈程度进行权衡。
4.  **`sync_binlog`：**
    *   **作用：** 控制binlog刷盘策略。
    *   **优化：**
        *   `1` (默认)：每次事务提交都将binlog同步到磁盘，最安全，但I/O最高。
        *   `0`：由操作系统决定何时刷盘。性能最好，但操作系统崩溃可能丢失binlog。
        *   `N` (N > 1)：每N次事务提交刷盘一次。在安全性和性能之间取得平衡。
    *   **选择：** 同样根据数据安全性和主从复制的严格性要求进行权衡。
5.  **`innodb_flush_method`：**
    *   **作用：** 控制InnoDB数据和日志文件刷盘的方式。
    *   **优化：**
        *   `O_DIRECT`：绕过操作系统文件系统缓存，直接写入磁盘。可以避免双重缓存（InnoDB Buffer Pool + OS Page Cache），减少内存压力，但可能增加直接I/O。对于有独立存储控制器缓存的系统，通常推荐使用。
        *   `fsync` (默认)：使用`fsync()`系统调用。
6.  **`tmp_table_size` 和 `max_heap_table_size`：**
    *   **作用：** 控制内存中临时表的大小。
    *   **优化：** 适当增大这两个参数，可以减少MySQL在执行复杂查询（如`GROUP BY`、`ORDER BY`、`UNION`等）时创建磁盘临时表的频率，从而减少I/O。
7.  **`sort_buffer_size` 和 `join_buffer_size`：**
    *   **作用：** 分别用于排序和连接操作的内存缓冲区。
    *   **优化：** 适当增大可以减少磁盘上的排序和连接操作。但这些是会话级别的参数，设置过大可能导致内存消耗过高。

#### 三、 数据库设计优化

1.  **合理的索引设计：**
    *   **最重要的一点。** 缺失或不当的索引会导致全表扫描，产生大量磁盘I/O。
    *   **覆盖索引：** 如果索引包含了查询所需的所有列，则无需回表查询数据行，大大减少I/O。
    *   **复合索引：** 考虑查询条件和排序的组合。
    *   **避免冗余索引和过多索引：** 索引本身也需要存储空间，并且会增加写入操作的I/O开销。
2.  **选择合适的数据类型：**
    *   使用占用空间最小但能满足需求的数据类型，例如，用`INT`代替`BIGINT`，用`VARCHAR`代替`TEXT`（如果长度可控）。
    *   更小的数据类型意味着更少的磁盘空间占用，每次I/O能读取更多行数据。
3.  **表结构优化：**
    *   **垂直拆分：** 将不常用的列或大文本列拆分到单独的表中。
    *   **水平拆分（Sharding）：** 将大表拆分成多个小表，分散I/O压力到不同的磁盘或服务器。
    *   **分区表：** 对于非常大的表，可以根据时间、ID等进行分区，将数据分散到不同的物理文件，提高查询效率和管理便利性。
4.  **数据归档：**
    *   将不活跃的历史数据归档到其他存储（如冷存储、数据仓库），减少主库的数据量，降低I/O压力。

#### 四、 SQL查询优化

1.  **避免全表扫描：** 确保`WHERE`子句、`JOIN`条件、`ORDER BY`和`GROUP BY`子句能够利用到索引。使用`EXPLAIN`分析查询计划。
2.  **优化`JOIN`操作：** 确保`JOIN`列有索引，并优化`JOIN`顺序。
3.  **减少`SELECT *`：** 只选择需要的列，减少数据传输量和I/O。
4.  **批量操作：**
    *   对于写入操作，尽量使用批量`INSERT`、`UPDATE`、`DELETE`，而不是单条操作。这可以减少事务开销和日志写入次数。
    *   例如：`INSERT INTO table (col1, col2) VALUES (v1, v2), (v3, v4);`
5.  **避免在`WHERE`子句中使用函数或对列进行计算：** 这会导致索引失效。
6.  **优化`LIMIT OFFSET`分页：** 对于大偏移量分页，可以考虑使用`WHERE id > last_id LIMIT N`的方式，避免全表扫描。

#### 五、 监控和分析

1.  **使用MySQL状态变量：**
    *   `SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool%';`：查看缓冲池命中率。
    *   `SHOW GLOBAL STATUS LIKE 'Innodb_data_reads%';` 和 `Innodb_data_writes%';`：查看数据读写量。
    *   `SHOW GLOBAL STATUS LIKE 'Binlog_cache_disk_use%';`：查看binlog是否频繁使用磁盘缓存。
    *   `SHOW ENGINE INNODB STATUS;`：提供详细的InnoDB状态信息，包括I/O、锁、事务等。
2.  **使用操作系统工具：**
    *   `iostat -x 1`：查看磁盘I/O的详细统计信息，如`%util`（磁盘利用率）、`r/s`（每秒读请求）、`w/s`（每秒写请求）、`rKB/s`（每秒读KB）、`wKB/s`（每秒写KB）、`await`（平均I/O等待时间）。
    *   `vmstat 1`：查看内存、交换、I/O、CPU活动。
    *   `top` / `htop`：查看进程CPU和内存使用情况。
3.  **慢查询日志：** 开启慢查询日志，分析执行时间长、扫描行数多的查询，这些往往是I/O瓶颈的来源。
4.  **Percona Toolkit：** `pt-diskstats`、`pt-query-digest`等工具可以帮助更深入地分析I/O和查询性能。

#### 总结

优化MySQL磁盘I/O是一个系统工程，需要综合考虑硬件、操作系统、MySQL配置、数据库设计和SQL查询。通常的优化步骤是：

1.  **监控和定位：** 找出是读I/O高还是写I/O高，是哪些查询或操作导致的高I/O。
2.  **硬件升级：** 如果预算允许，升级到SSD/NVMe是最直接有效的。
3.  **MySQL配置调整：** 优先调整`innodb_buffer_pool_size`、`innodb_log_file_size`等核心参数。
4.  **索引和查询优化：** 这是长期且持续的工作，对读I/O影响最大。
5.  **数据库设计优化：** 考虑分区、拆分等。

在进行任何配置更改之前，务必在测试环境中进行充分的测试，并做好备份，以避免生产环境出现意外。
	
### 索引的数据结构

索引页中记录的是页的最小主键id和页号
索引结构不会影响单表的最大行数，2000W只是推荐值，但是超过这个数可能使树的层级更高，影响查询效率


### **什么是二次索引？**

*   **定义**：在数据库表中，除了主键（Primary Key）自动创建的索引（通常是聚簇索引或唯一索引）之外，在其他列或列组合上创建的索引都称为二次索引。通常指的是**非主键索引**，也称为**辅助索引**（Secondary Index）或**普通索引**。它是在表中的非主键列上创建的索引，目的是为了加快对这些列的查询速度。
*   **作用**：
    *   **加速查询**：当你在 `WHERE` 子句中使用被索引的列时，数据库可以快速定位到符合条件的行，而无需全表扫描。
    *   **加速排序和分组**：如果 `ORDER BY` 或 `GROUP BY` 子句中使用了索引列，数据库可以直接利用索引的有序性，避免额外的排序操作。
    *   **加速连接（JOIN）**：在 `JOIN` 操作中，如果连接条件涉及被索引的列，可以显著提高连接效率。

---

#### **如何设置二次索引（SQL 语法示例）**

设置二次索引主要通过 `CREATE INDEX` 语句或 `ALTER TABLE` 语句来完成。以下是一些常见数据库的示例：

假设你有一个 `users` 表，包含 `id` (主键), `name`, `email`, `city` 等列。

##### **1. 创建普通二次索引 (Non-Unique Index)**

允许索引列中存在重复值。

**通用语法：**
```sql
CREATE INDEX index_name ON table_name (column_name);
```
或针对多列的复合索引：
```sql
CREATE INDEX index_name ON table_name (column1, column2, ...);
```

**示例：在 `users` 表的 `city` 列上创建索引**

*   **MySQL / PostgreSQL / SQL Server:**
    ```sql
    CREATE INDEX idx_users_city ON users (city);
    ```
    或者使用 `ALTER TABLE` (MySQL 常用):
    ```sql
    ALTER TABLE users ADD INDEX idx_users_city (city);
    ```

##### **2. 创建唯一二次索引 (Unique Index)**

确保索引列中的所有值都是唯一的（NULL 值通常可以重复，具体取决于数据库实现）。

**通用语法：**
```sql
CREATE UNIQUE INDEX index_name ON table_name (column_name);
```

**示例：在 `users` 表的 `email` 列上创建唯一索引**

*   **MySQL / PostgreSQL / SQL Server:**
    ```sql
    CREATE UNIQUE INDEX idx_users_email ON users (email);
    ```
    或者使用 `ALTER TABLE` (MySQL 常用):
    ```sql
    ALTER TABLE users ADD UNIQUE INDEX idx_users_email (email);
    ```

##### **3. 创建复合索引 / 联合索引 (Composite Index)**

在多个列上创建索引，这些列的顺序很重要。查询时，只有当查询条件从索引的第一个列开始匹配时，才能有效利用复合索引。

**示例：在 `users` 表的 `city` 和 `name` 列上创建复合索引**

*   **MySQL / PostgreSQL / SQL Server:**
    ```sql
    CREATE INDEX idx_users_city_name ON users (city, name);
    ```
    这个索引可以加速以下查询：
    *   `WHERE city = 'Beijing'`
    *   `WHERE city = 'Beijing' AND name = 'John'`
    *   但对 `WHERE name = 'John'` 的查询效果不佳。

---

#### **设置二次索引的考量和最佳实践**

创建二次索引并非越多越好，需要权衡利弊：

1.  **选择合适的列：**
    *   **频繁用于 `WHERE` 子句的列**：这是最常见的索引场景。
    *   **`JOIN` 条件中的列**：连接操作的效率会显著提升。
    *   **`ORDER BY` 或 `GROUP BY` 中的列**：可以避免额外的排序操作。
    *   **区分度高（Cardinality 高）的列**：即列中不重复的值越多，索引效果越好。例如，`性别`（只有男/女）这种区分度低的列不适合单独建立索引。
    *   **避免过度索引**：每个索引都会占用磁盘空间，并且在数据插入、更新、删除时需要额外维护，这会降低写入性能。

2.  **索引类型选择：**
    *   **普通索引**：最常用，适用于大多数查询加速场景。
    *   **唯一索引**：除了加速查询，还能强制保证列的唯一性，作为业务约束。
    *   **复合索引**：当查询条件经常涉及多个列的组合时考虑。注意列的顺序，将最常用的或区分度最高的列放在前面。
    *   **覆盖索引（Covering Index）**：如果一个查询所需的所有列都包含在索引中，那么数据库可以直接从索引中获取数据，而无需回表（访问实际的数据行），这能极大提升查询性能。例如，`SELECT email FROM users WHERE city = 'Beijing'`，如果 `idx_users_city_email` (city, email) 是一个复合索引，那么它就是一个覆盖索引。

3.  **维护成本：**
    *   **存储空间**：索引本身需要占用额外的磁盘空间。
    *   **写入性能**：每次对表进行 `INSERT`、`UPDATE`、`DELETE` 操作时，数据库都需要同步更新相关的索引，这会增加写操作的开销。因此，对于写操作远多于读操作的表，需要谨慎创建索引。

4.  **何时不适合创建索引：**
    *   **数据量很小的表**：全表扫描可能比索引查找更快。
    *   **频繁更新的列**：如果一个列经常被更新，而查询不频繁，索引的维护成本可能高于其带来的收益。
    *   **区分度很低的列**：如布尔值、枚举值很少的列。
    *   **不用于查询条件的列**：如果一个列从不出现在 `WHERE`、`ORDER BY`、`GROUP BY` 或 `JOIN` 中，则无需为其创建索引。

通过合理地设置二次索引，可以显著提升数据库的查询性能，优化应用程序的响应速度。

### 优化索引的方法

#### 前缀索引优化

前缀索引：
- 只索引字符串列的前N个字符
    
- 而不是索引整个字符串值
    
- 通过`INDEX(column_name(N))`语法创建（N表示前缀长度）

优点：
- 减少索引存储空间：索引体积更小
    
- 加快索引扫描速度：更小的索引意味着更高的缓存命中率
    
- 支持无法完整索引的大字段：如TEXT/BLOB类型通常不能整个被索引

缺点和注意事项
- 不适合后缀查询(`LIKE '%suffix'`)
    
- 可能增加重复键值风险
    
- 排序操作可能无法使用前缀索引
	
- InnoDB引擎前缀索引最长767字节(utf8mb4约191字符)
    
- 主键不能使用前缀索引
    
- 唯一索引使用前缀索引时要确保前缀的唯一性
	
- order by 不能使用前缀索引
	
- 不能把前缀索引用作覆盖索引

```sql
-- MySQL中创建前缀索引
ALTER TABLE users ADD INDEX idx_name (name(5));  -- 只索引name前5个字符

-- 创建表时指定
CREATE TABLE products (
    product_code VARCHAR(100),
    INDEX idx_code (product_code(10))
);
```
#### 覆盖索引优化

**覆盖索引**（Covering Index）是指：查询可以**完全通过索引**获取所需数据，而**无需访问表数据行**（避免回表操作）。
- 一个索引包含了查询需要的所有字段
    
- 执行查询时只需访问索引，不需回表查数据页
    
- 在EXPLAIN结果中显示"Using index"

优点：
1. **减少I/O操作**：只读取索引数据，不读数据页
    
2. **减少CPU消耗**：避免了解析完整行数据
    
3. **利用索引顺序**：索引通常比表数据更紧凑有序
    
4. **缓冲池效率**：更小的索引更容易被缓存在内存中

查询要成为覆盖查询必须满足：
- SELECT列表中的列都包含在索引中
    
- WHERE条件使用的列是索引的前导列
    
- 任何需要的排序列也在索引中

假设一个表有100万行，索引和数据分离存储：

| 查询类型 | 需要访问  | 估计I/O次数   | 执行时间     |
| ---- | ----- | --------- | -------- |
| 覆盖查询 | 仅索引   | 1-5次      | 1-5ms    |
| 普通查询 | 索引+数据 | 100-1000次 | 10-100ms |

限制与注意事项
1. 索引维护成本：覆盖索引包含更多列，会增大索引体积
    
2. 更新代价：表数据变更时需要维护更多索引数据
    
3. 存储空间：额外的索引需要更多磁盘空间
    
4. 不是所有查询都能转化为覆盖查询

```sql
-- 表结构
CREATE TABLE users (
    id INT PRIMARY KEY,
    username VARCHAR(50),
    age INT,
    INDEX idx_username_age (username, age)
);

-- 覆盖查询示例1（使用复合索引）
EXPLAIN SELECT username, age FROM users WHERE username = 'john';
-- 结果会显示"Using index"

-- 覆盖查询示例2（使用主键）
EXPLAIN SELECT id FROM users WHERE id > 100;
-- 主键索引天然是覆盖索引

-- 非覆盖查询示例
EXPLAIN SELECT username, age, id FROM users WHERE username = 'john';
-- 虽然用了索引，但需要回表取id值
```

#### 主键索引最好是自增的

自增主键按顺序插入，为追加操作，不需要移动数据
非自增主键可能会发生页分裂，造成大量内存碎片，导致索引结构不紧凑，影响查询效率

主键字段越小，二级索引的叶子结点越小，所占空间越小

- 索引最好设置NOT NULL
	- 有NULL值会导致优化器在做索引选择的时候更加复杂，难以优化
	- NULL是没有意义的值但是会占用物理空间，带来存储空间的问题

常见扫描类型的执行效率从低到高的顺序为： 
All（全表扫描）； 
index（全索引扫描）； 
range（索引范围扫描）； 
ref（非唯一索引扫描）； 
eq_ref（唯一索引扫描）； 
const（结果只有一条的主键或唯一索引扫描）。

#### 防止索引失效

发生索引失效的情况
- 当我们使用左或者左右模糊匹配的时候，也就是 like %xx 或者 like %xx%这两种方式都会造成索引失效； 
- 当我们在查询条件中对索引列做了计算、函数、类型转换操作（自动把字符串转换为数字再进行比较），这些情况下都会造成索引失效； 如果索引列的类型是字符串但是条件查询的时候输入 的是数字会发生隐式类型转换，会涉及CAST函数（对索引列使用了函数）
- 联合索引要能正确使用需要遵循最左匹配原则，也就是按照最左优先的方式进行索引的匹配，否则就会导致索引失效。 
- 在 WHERE 子句中，如果在 OR 前的条件列是索引列，而在 OR 后的条件列不是索引列，那么索引会失效。就会进行全表扫描

![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250816131046078.png)


### 索引下推

**索引下推**（Index Condition Pushdown）是指：
	
- 将WHERE条件中**索引相关部分**下推到存储引擎层执行
    
- 存储引擎在读取索引时就进行条件过滤
    
- 减少传给服务器层的数据量
    
- 在EXPLAIN中显示"Using index condition"

 没有ICP的传统流程
	
1. 存储引擎读取索引元组
    
2. 通过索引定位表记录（回表）
    
3. 服务器层对返回的数据应用WHERE条件过滤
    

 启用ICP的流程
	
1. 存储引擎读取索引元组
    
2. **在存储引擎层**评估WHERE条件中可下推的部分
    
3. 只对满足条件的索引项执行回表操作
    
4. 服务器层处理最终结果

ICP优化适用于以下场景：

- 需要访问表记录的查询（非覆盖查询）
    
- WHERE条件包含索引列和非索引列
    
- 特别是复合索引的非前导列条件
    
- InnoDB和MyISAM引擎支持

优势：
1. **减少回表次数**：只对真正符合条件的记录回表
    
2. **降低I/O压力**：减少了从存储引擎到服务器的数据传输
    
3. **提高缓存效率**：更少的数据被加载到内存中

限制条件
1. 只适用于二级索引（非聚簇索引）
    
2. 不适用于主键/聚簇索引查询
    
3. 虚拟列上的二级索引不支持ICP
    
4. 子查询、存储函数条件不能下推
    
5. 引用子表的条件不能下推

```sql
-- 表结构
CREATE TABLE employees (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    dept VARCHAR(50),
    INDEX idx_name_age (name, age)
);

-- 查询示例
SELECT * FROM employees 
WHERE name LIKE '张%' AND age > 25;

-- 没有ICP时：
-- 1. 先通过索引找到所有name以'张'开头的记录
-- 2. 对所有找到的记录回表取完整数据
-- 3. 在服务器层过滤age>25的记录

-- 启用ICP时：
-- 1. 通过索引找到name以'张'开头的记录
-- 2. 在存储引擎层直接检查这些记录的age>25条件
-- 3. 只对满足两个条件的记录回表
```


「变长字段长度列表」中的信息之所以要逆序存放，是因为这样可以使得位置靠前的记录的真实数据和数据对应的字段长度信息可以同时在一个+cpu+cache+line+中，这样就可以提高+cpu+cache+的命中率。

### MySQL的NULL值是怎么存放的?

MySQL的Compact行格式中会用「NULL值列表」来标记值为NULL的列,NULL值并不会存储在行格式中的真实数据部分。

NULL 列表会占用1字节空间,当表中所有字段都定义成NOT NULL,行格式中就不会有NULL值列表,
这样可节省1字节的空间。

### MySQL怎么知道 varchar(n)实际占用数据的大小?

MySQL的Compact行格式中会用「变长字段长度列表」存储变长字段实际占用的数据大小。
<=255: 1；>255：2

### varchar(n)中n最大取值为多少?

一行记录最大能存储65535字节的数据,但是这个是包含「变长字段字节数列表所占用的字节数」和
「NULL值列表所占用的字节数」。所以,我们在算varchar(n)中n最大值时,需要减去这两个列表所占用的字节数。

如果一张表只有一个varchar(n)字段,且允许为NULL,字符集为ascii。varchar(n)中n最大取值为
65532。

计算公式:65535-变长字段字节数列表所占用的字节数-NULL值列表所占用的字节数=65535-2-1=
65532。

如果有多个字段的话,要保证所有字段的长度+变长字段字节数列表所占用的字节数+NULL值列表所占用的字节数 <= 65535。

### 行溢出后,MySQL是怎么处理的?

如果一个数据页存不了一条记录,InnoDB存储引擎会自动将溢出的数据存放到「溢出页」中。

Compact 行格式针对行溢出的处理是这样的:当发生行溢出时,在记录的真实数据处只会保存该列的一部分数据,而把剩余的数据放在「溢出页」中,然后真实数据处用20字节存储指向溢出页的地址,从而可
以找到剩余数据所在的页。

compressed和dynamic采取不同的方法，在记录的真实数据处只会用20字节保存溢出页的地址，把全部数据都放在溢出页中，

### 如何优化count（* ）？

1. 近似值：使用explain指令或show table status来对表进行估算
2. 额外表保存计数值：在数据表插入数据的同时在额外表同时加1，需要额外维护

### **B+树
	一种自平衡的树数据结构，它是B树的变体，广泛应用于数据库和文件系统中的索引结构。
核心特点：**

1.  **所有数据都存储在叶子节点中**：
    *   非叶子节点（内部节点）只存储键值（索引）和指向子节点的指针，不存储实际的数据记录。
    *   叶子节点存储了所有的数据记录（或指向数据记录的指针），并且这些数据记录是按键值大小顺序排列的。

2.  **叶子节点之间通过链表连接**：
    *   所有叶子节点构成一个有序链表，这使得范围查询（如查找某个范围内的所有记录）非常高效，只需找到起始叶子节点，然后沿着链表遍历即可。

3.  **所有叶子节点都位于同一层**：
    *   这意味着从根节点到任何一个数据记录的查找路径长度都是相同的，保证了查询性能的稳定性。

4.  **非叶子节点作为索引**：
    *   非叶子节点中的键值是其子节点中最小（或最大）键值的副本，用于引导搜索方向。

---

#### **B+树的结构示意**

假设一个B+树的阶为 `m`（即一个节点最多有 `m-1` 个键和 `m` 个子节点）：

*   **根节点**：至少有2个子节点。
*   **非叶子节点**：
    *   包含 `k` 个键值和 `k+1` 个子节点指针。
    *   `ceil(m/2) - 1 <= k <= m-1` (除了根节点)。
    *   键值用于分隔子节点的范围。
*   **叶子节点**：
    *   包含 `k` 个键值和对应的数据记录（或指向数据记录的指针）。
    *   `ceil(m/2) - 1 <= k <= m-1`。
    *   叶子节点之间有指针连接，形成有序链表。

---

#### **B+树的操作**

1.  **查找（Search）**：
    *   从根节点开始，根据键值在当前节点中进行二分查找，确定进入哪个子节点。
    *   重复此过程，直到到达叶子节点。
    *   在叶子节点中找到对应的键值和数据记录。
    *   对于范围查询，找到起始叶子节点后，沿着叶子节点的链表遍历即可。

2.  **插入（Insertion）**：
    *   首先查找要插入的键值，找到对应的叶子节点。
    *   将键值和数据插入到叶子节点中。
    *   如果叶子节点空间足够，则完成插入。
    *   如果叶子节点已满，则进行**分裂**：将叶子节点一分为二，并将中间的键值（或其副本）提升到父节点中。
    *   如果父节点也满了，则继续向上分裂，直到根节点。如果根节点也分裂，树的高度会增加。

3.  **删除（Deletion）**：
    *   首先查找要删除的键值，找到对应的叶子节点。
    *   从叶子节点中删除键值和数据。
    *   如果删除后叶子节点中的键值数量低于最小限制（下溢），则尝试从兄弟节点**借用**键值。
    *   如果无法借用，则与兄弟节点**合并**，并将父节点中对应的键值删除。
    *   合并操作可能导致父节点下溢，从而向上级联，直到根节点。如果根节点只剩一个子节点，则树的高度会降低。

---

#### **B+树的优势**

1.  **高效的磁盘I/O**：
    *   B+树的节点大小通常设计为与磁盘块大小一致，这样每次磁盘I/O可以读取一个完整的节点，减少了磁盘寻道次数。
    *   由于非叶子节点不存储数据，相同大小的节点可以存储更多的键值，从而降低了树的高度，进一步减少了磁盘I/O次数。

2.  **支持高效的范围查询**：
    *   叶子节点通过链表连接，使得范围查询非常高效，只需一次查找定位到起始点，然后顺序遍历链表即可。

3.  **查询性能稳定**：
    *   所有数据都在叶子节点，且所有叶子节点都在同一层，因此任何查询的路径长度都相同，查询时间复杂度稳定。

4.  **适用于数据库和文件系统**：
    *   其结构特点非常适合存储在磁盘上的大量数据，是数据库索引（如MySQL的InnoDB存储引擎）和文件系统（如NTFS、HFS+）的首选结构。

---

#### **B+树与B树的区别**

| 特性        | B树                                    | B+树                                                                                                        |
| --------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| **数据存储**  | 内部节点和叶子节点都存储数据                        | 只有叶子节点存储数据，内部节点只存储键值                                                                                       |
| **内部节点**  | 包含键值和数据指针                             | 只包含键值和子节点指针                                                                                                |
| **叶子节点**  | 不一定在同一层，不通过链表连接                       | 都在同一层，并通过链表连接                                                                                              |
| **查询效率**  | 单个查询可能更快（数据可能在内部节点）                   | 范围查询更高效，所有查询路径长度一致                                                                                         |
| **磁盘I/O** | 内部节点存储数据，可能导致更多I/O                    | 内部节点更小，相同I/O可加载更多索引，树更矮                                                                                    |
| 插入和删除效率   | B树没有冗余节点，删除节点时可能会使树形发生变化              | B+树有冗余节点（所有非叶子节点都是冗余索引），因此删除的时候可以直接在叶子节点删除，不需要改变非叶子节点；<br>插入可能存在节点分裂（如果节点饱和），但是只涉及一条路径，还有自动平衡功能，类似红黑树的旋转操作 |
| 范围查询      | 叶子节点没有链表连接，只能通过遍历树进行查询，会涉及多个节点的磁盘IO操作 | 叶子节点通过链表连接，查询高效                                                                                            |

| 特性/数据结构   | B+树 (B+ Tree)        | 哈希表 (Hash Table) | 红黑树 (Red-Black Tree)        |
| :-------- | :------------------- | :--------------- | :-------------------------- |
|           |                      |                  | 自平衡二叉查找树                    |
| **主要优化**  | 磁盘I/O，范围查询           | 精确匹配，平均O(1)      | 内存中平衡，O(log N)操作，有序         |
| **存储介质**  | 磁盘（主要）               | 内存（主要）           | 内存（主要）                      |
| **单点查询**  | O(log N) (磁盘I/O少)    | 平均O(1)，最坏O(N)    | O(log N)                    |
| **范围查询**  | **非常高效** (叶子链表)      | **不支持**          | 支持 (O(log N) + 结果数量)        |
| **插入/删除** | O(log N) (可能涉及分裂/合并) | 平均O(1)，最坏O(N)    | O(log N) (涉及旋转/颜色调整)        |
| **数据顺序**  | 有序 (叶子节点链表)          | 无序               | 有序 (中序遍历)                   |
| **空间效率**  | 较高 (内部节点只存键值)        | 较高 (但可能因冲突处理而增加) | 较高 (每个节点额外存储颜色信息)           |
| **典型应用**  | 数据库索引、文件系统           | 缓存、字典、符号表        | 内存数据库、有序集合、语言库中的`map`/`set` |
总而言之，B+树通过将所有数据集中到叶子节点并用链表连接，极大地优化了范围查询和磁盘I/O效率，使其成为处理大量数据的理想索引结构。


### 分页查询

分页查询是数据库操作中非常常见的需求，用于从大量数据中分批获取结果，以提高用户体验和系统性能。不同的数据库系统有不同的分页语法。

分页查询通常需要两个参数：

1.  **页码 (Page Number)**：当前要获取第几页的数据。
2.  **每页大小 (Page Size / Rows Per Page)**：每页显示多少条数据。

通过这两个参数，我们可以计算出：

*   **偏移量 (Offset)**：从结果集的第几条记录开始取。
    `Offset = (Page Number - 1) * Page Size`
*   **限制数量 (Limit)**：总共取多少条记录。
    `Limit = Page Size`

---

#### **1. MySQL

**语法：**
```sql
SELECT column1, column2, ...
FROM your_table
WHERE conditions
ORDER BY column_to_sort
LIMIT page_size OFFSET offset_value;
```

**示例：**
假设每页显示10条记录，我们想获取第3页的数据。
*   `Page Number = 3`
*   `Page Size = 10`
*   `Offset = (3 - 1) * 10 = 20`
*   这条语句会跳过前20条记录，然后返回接下来的10条记录。

**简化写法 (MySQL 常用):**
`LIMIT offset_value, page_size`
```sql
SELECT id, name, age
FROM users
WHERE status = 'active'
ORDER BY id ASC
LIMIT 20, 10;
```

#### **分页查询的性能考量**

*   **`ORDER BY` 子句**：分页查询几乎总是需要 `ORDER BY` 来确保结果的顺序性。如果 `ORDER BY` 的列没有索引，数据库可能需要进行文件排序，这会非常耗时。因此，确保 `ORDER BY` 的列有合适的索引至关重要。
*   **大偏移量问题**：当 `OFFSET` 值非常大时（例如，获取第10000页的数据），数据库仍然需要扫描或跳过前面大量的记录，这会导致性能下降。
    *   **优化方案1 (基于上次查询的ID)**：对于“下一页”/“上一页”的场景，可以记录上一页最后一条记录的ID（或排序字段），然后使用 `WHERE id > last_id LIMIT page_size` 的方式，避免使用大偏移量。
        ```sql
        -- 获取下一页，假设上一页最后一条记录的ID是 12345
        SELECT id, name, age
        FROM users
        WHERE id > 12345 AND status = 'active'
        ORDER BY id ASC
        LIMIT 10;
        ```
        这种方式需要 `id` 列有索引，并且 `id` 是唯一的且递增的。
    *   **优化方案2 (覆盖索引)**：如果查询的列都在索引中，可以利用覆盖索引减少回表操作。
    *   **优化方案3 (业务限制)**：在某些业务场景下，可以限制用户只能查看前几百页的数据，避免无限翻页。

 


### **1. 脏读 (Dirty Read)**

*   **定义**：一个事务读取了另一个**未提交**事务的数据。如果那个未提交的事务最终回滚（Rollback），那么第一个事务读取到的数据就是“脏”数据，因为它从未真正存在于数据库中。
*   **场景**：
    1.  事务A更新了一条记录，但尚未提交。
    2.  事务B读取了事务A更新后的这条记录。
    3.  事务A由于某种原因回滚了，它对数据的修改被撤销。
    4.  事务B读取到的数据是无效的，因为它基于一个最终被撤销的修改。
*   **示例**：
    *   **初始状态**：账户A余额1000元。
    *   **事务T1**：`UPDATE accounts SET balance = 900 WHERE id = 'A';` (未提交)
    *   **事务T2**：`SELECT balance FROM accounts WHERE id = 'A';` (读到900)
    *   **事务T1**：`ROLLBACK;` (账户A余额恢复1000)
    *   **结果**：事务T2读到了900，但这个900从未真正持久化，是“脏”数据。
*   **隔离级别**：
    *   **读未提交 (Read Uncommitted)** 隔离级别允许脏读。
    *   **读已提交 (Read Committed)** 及更高隔离级别可以避免脏读。

---

### **2. 不可重复读 (Non-Repeatable Read)**

*   **定义**：一个事务在**同一条记录**上，两次读取的结果不一致。这通常是由于在第一次读取之后，另一个**已提交**的事务修改了这条记录。
*   **场景**：
    1.  事务A读取了一条记录。
    2.  事务B修改了这条记录并提交。
    3.  事务A再次读取同一条记录，发现数据与第一次读取时不同。
*   **示例**：
    *   **初始状态**：账户A余额1000元。
    *   **事务T1**：`SELECT balance FROM accounts WHERE id = 'A';` (读到1000)
    *   **事务T2**：`UPDATE accounts SET balance = 900 WHERE id = 'A';` `COMMIT;`
    *   **事务T1**：`SELECT balance FROM accounts WHERE id = 'A';` (再次读到900)
    *   **结果**：事务T1在同一个事务内，对同一条记录的两次读取结果不一致。
*   **隔离级别**：
    *   **读已提交 (Read Committed)** 隔离级别允许不可重复读。
    *   **可重复读 (Repeatable Read)** 及更高隔离级别可以避免不可重复读。

---

### **3. 幻读 (Phantom Read)**

*   **定义**：一个事务在**同一范围的查询**中，两次查询的结果集不一致。这通常是由于在第一次查询之后，另一个**已提交**的事务插入或删除了符合查询条件的**新行**。
*   **场景**：
    1.  事务A执行一个范围查询（例如，`SELECT COUNT(*)` 或 `SELECT *`）。
    2.  事务B插入或删除了符合事务A查询条件的新行，并提交。
    3.  事务A再次执行相同的范围查询，发现结果集中的行数或具体行发生了变化。
*   **示例**：
    *   **初始状态**：`students` 表有 (ID:1, Name:'Alice'), (ID:2, Name:'Bob')。
    *   **事务T1**：`SELECT COUNT(*) FROM students WHERE ID > 0;` (读到2条)
    *   **事务T2**：`INSERT INTO students (ID, Name) VALUES (3, 'Charlie');` `COMMIT;`
    *   **事务T1**：`SELECT COUNT(*) FROM students WHERE ID > 0;` (再次读到3条)
    *   **结果**：事务T1在同一个事务内，对相同范围的查询结果集发生了变化，出现了“幻影”行。
*   **隔离级别**：
    *   **可重复读 (Repeatable Read)** 隔离级别在某些数据库（如MySQL的InnoDB）中通过**间隙锁（Gap Lock）可以避免幻读**，但在标准SQL定义中，可重复读是允许幻读的。
	    * 快照读（普通select语句）：通过MVCC方式解决幻读
	    * 当前读（select... for update 等语句）：通过next-key lock（记录锁+间隙锁）方式解决幻读。事务A执行锁定读语句后，事务执行插入语句时判断到插入的位置被事物A加了锁，于是事物B会生成一个插入意向锁，同时进入等待状态，直到事务A提交了事务。这就避免了事务B插入新纪录使事务A发生幻读的现象
    *   **串行化 (Serializable)** 隔离级别可以完全避免幻读。

---

### **事务隔离级别与一致性问题**

下表总结了SQL标准定义的四种隔离级别以及它们能解决的一致性问题：

| 隔离级别（定义及实现）级别越高性能越差                                                       | 脏读 (Dirty Read) | 不可重复读 (Non-Repeatable Read) | 幻读 (Phantom Read)              |
| :------------------------------------------------------------------------ | :-------------- | :-------------------------- | :----------------------------- |
| **读未提交 (Read Uncommitted)** 事务未提交前的变更可被其他事务看到                             | 允许              | 允许                          | 允许                             |
| **读已提交 (Read Committed)** 事务提交之后变更才能被看到。在【每个语句执行前】重新生成**Read View**       | 避免              | 允许                          | 允许                             |
| **可重复读 (Repeatable Read)** 事务执行过程中看到的数据与启动前的一致。在【启动事务时】生成一个**Read View**。 | 避免              | 避免                          | 允许 (标准SQL) / 避免 (MySQL InnoDB) |
| **串行化 (Serializable)** 对记录加上**读写锁**，发生读写冲突时，后访问的事务要等前一个事务结束才能进行操作         | 避免              | 避免                          | 避免                             |

**总结**：

*   **脏读**：读到未提交的数据。
*   **不可重复读**：两次读取同一行数据，结果不同（数据被修改）。
*   **幻读**：两次读取同一范围数据，结果集不同（数据被增删）。

### Read View在MVCC里如何工作？

- Read View有四个重要字段
	- m_ids：在创建Read View时，当前数据库中【活跃事务】（启动但未提交的事务）的事务id**列表**
	- min_trx_id：在创建Read View时，当前数据库中【活跃事务】中事务id最小的事务
	- max_trx_id：全局事务中最大的事务id值+1；创建Read View时当前数据库中应该给下一个事务的id值
	- creator -trx_id：创建该Read View的事务的事务id
	- 使用InnoDB存储引擎的数据库表的聚簇索引记录中包含隐藏列（trx_id, roll_pointer)![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818172337022.png)
- 一个事务去访问记录时，除了自己更新的记录可见之外
	- 该版本的记录对当前事务**可见**：
		- 记录的trx_id < min_trx_id ：这个版本的记录是在创建Read View前已提交的事务生成的
		- min_trx_id < trx_id < max_trx_id：记录的 trx_id **不在** m_ids 列表中，表示生成该版本记录的活跃事务已经被提交
	- 该版本的记录对当前事务**不可见**：
		- 记录的trx_id > max_trx_id ：这个版本的记录是在创建Read View后才启动的事务生成的
		- min_trx_id < trx_id < max_trx_id：记录的 trx_id **在** m_ids 列表中，表示生成该版本记录的活跃事务依然活跃（未被提交事务）

### 可重复读是如何工作的？

可重复读隔离级别是在启动事务时生成一个Read View ，然后整个事务都在使用这个Read View 。

![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818174348374.png)

1. 事务B读取小林的账户余额为100万：读取trx_id ，小于事务B的Read View 中的min_trx_id ，表示修改这条记录的事务早就在事务B启动前提交过了，于是该版本的记录对事物B可见
2. 事务A将余额修改为200万，但是未提交事务：trx_id 为事务A的事务id（51）。 MySQL会记录undolog，以链表方式串联，形成版本链，roll_pointer 指向历史版本，
3. 事务B读取余额为100万：读取trx_id（51），在事务B的Read View 中的min_trx_id 和 max_trx_id 之间，判断 trx_id 值是否在m_ids 范围内。在则说明该记录是被未提交的事务修改的，对事物B不可见，于是沿着undo log 链条往下找旧版本的记录，直到找到trx_id 小于事务B的Read View 中的min_trx_id 的第一条记录，进行读取
4. 事务A提交事务
5. 事务B读取余额为100万：事务B的Read View 中的 m_ids 的事务不变，因此还跟之前判断一样，读取100万

普通的select语句基于MVCC实现快照读，不会加锁。普通的 `SELECT` 语句在“读已提交（RC）”和“可重复读（RR）”隔离级别下是**非阻塞读**。它直接读取 MVCC 中的版本链，不需要获取任何锁（如行锁、表锁）。这使得读操作不会阻塞写操作，写操作也不会阻塞读操作，极大地提高了并发性能。
	可能会发生的问题：**丢失更新**：根本原因：SET一个绝对值（即执行阶段的盲目性）数据库无法保证业务级别的一致性
	比如：初始库存是100，事务A增加30，事务B扣减50，两个事务几乎同时发生，两个事务读到的库存都是100，在事务提交之前读到的库存都是100，事务A和B都扣减库存但是未提交，之后A线提交，库存更新为130，之后B提交，库存更新为50，最终数据库中的库存为50，与预期值80严重不符。
	事务A的更新完全丢失，向从未发生过一样
	 。
	原因：两个事务的读操作都是快照读，不会加锁，因此读取到的是同一个历史版本，也不会相互阻塞。之后的业务逻辑判断也是基于这个相同的旧值进行的。在写操作中，本来update语句本身是当前读，会获取锁并读取最新数据。
	t5时刻：事务B会先定位记录根据索引进行当前读，找到记录的最新提交版本，如果大小被活跃事务修改则Innodb引擎会检查记录的上锁情况。事务A还未提交，事务A对于这条记录加了一个独占锁X 锁。所以事务B无法对该记录加锁，被放入锁等待队列，挂起并等待事务A释放锁。
	事务A提交之后，会释放它持有的锁，Innodb会唤醒正在等待的事务B。事务B立即获取锁，执行当前读，在执行SET操作。但是update语句中SET子句中的值是基于旧值计算好的，是一个硬编码的值，数据库引擎在执行update的时候执行逻辑是根据索引找到记录并加锁，之后不会去检查因为SET之间将值设为了一个固定值，
	因此，数据库就不管是否有更新直接进行修改。直接修改了新值，忽视了从读到写阶段数据可能被修改。
	-- 应用层：new_balance = old_balance - 50
	UPDATE accounts **SET balance = 50** WHERE id = 1; -- 值50是“绝对”的
	但是如果改成**SET balance = balance - 50**的相对命令就不会发生丢失更新
	.
	在数据库中完成决策，将读计算写三个操作合并成一个原子性的SQL操作。决策和执行都在数据库内部基于最新数据瞬间完成**SET balance = balance - 50**
	在需要基于查询结果进行更新的业务中必须使用悲观锁（SELECT...FOR UPDATE）或者乐观锁（更新时带上版本号作为条件）来代替简单的快照读，以确保数据的一致性。

| 时间线 | 事务A                       | 事务B                       | 数据库真实值                    | 事务A看到的视图 | 事务B看到的视图 |
| --- | ------------------------- | ------------------------- | ------------------------- | -------- | -------- |
| t0  | `START TRANSACTION;` (T1) |                           | 100                       | 生成快照     |          |
| t1  |                           | `START TRANSACTION;` (T1) | 100                       |          | 生成快照     |
| t2  | `SELECT ... ;` -> **100** |                           | 100                       | **100**  | **100**  |
| t3  |                           | `SELECT ... ;` -> **100** | 100                       | **100**  | **100**  |
| t4  | `UPDATE ... SET 50;`      |                           | **50** (A未提交)             | **100**  | **100**  |
| t5  |                           | `UPDATE ... SET 130;`     | **130** (B未提交)            | **100**  | **100**  |
| t6  | `COMMIT;`                 |                           | **50** (A提交)              | **结束**   | **100**  |
| t7  |                           | `COMMIT;`                 | **130** (B提交，**覆盖了A的更新**) |          | **结束**   |

select ... for update 语句是当前读，每次读都是拿到最新版本的数据，会对读到的记录加上next-key lock锁

### 读提交是如何工作的？

读提交隔离级别是在每次读取数据时都会生成一个新的Read View 

1. 事务B读取（创建Read View）余额为100万
2. 事务A修改（未提交）余额为200万
3. 事务B读取（创建Read View）余额为100万
4. 事务A提交事务
5. 事务B读取（创建Read View）余额为200万：创建的Read View 中的 m_ids 的事务更新为52，min_trx_id 更新为52。这条记录的 trx_id < min_trx_id ，表示修改这条记录的事务早在创建Read View 之前提交，所以该版本的记录对事物B可见

### 幻读被完全解决了吗？

没有：可重复读隔离级别下，事务A第一次执行普通select语句时生成一个ReadView，之后事务B插入一条id=5的记录并提交。事务A对这条记录进行了Update更新操作（update会看到已提交的最新数据，不受最初Read View 的限制），这条新记录的trx_id隐藏列的值就变成了事务A的事务id，之后事务A再使用普通select语句去查询这条记录时就可以看到这条记录，于是发生了幻读
	设计考虑：数据一致性（事务更新一条记录后应该能够看到自己的更新）；现实需求（业务逻辑中经常需要先查后改，如果看不到自己的修改会看奇怪）；性能考量（update需要基于最新数据否则可能更新错误的数据）
	避免：使用select for update，或者使用串行化隔离级别，或者保证不会在第一次读之后有事务插入数据
	![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818181814207.png)

除了上面这一种场景会发生幻读现象之外,还有下面这个场景也会发生幻读现象。

· T1时刻:事务A先执行「快照读语句」:select * from t_test where id>100得到了3条记录。
· T2时刻:事务B往插入一个id=200的记录并提交;
· T3时刻:事务A再执行「当前读语句」select * from t_test where id>100 for update 就会得到4条记录,此时也发生了幻读现象。

要避免这类特殊场景下发生幻读的现象的话,就是尽量在开启事务之后,马上执行 select ... for update这类当前读的语句,因为它会对记录加 next-key lock,从而避免其他事务插入一条新记录。


### 全局锁：
命令：`flush tables with read lock`；执行后进入只读状态
释放锁：`unlock table`
- 应用场景：全库逻辑备份
- 缺点：造成业务停滞；如果数据库有很多数据备份会花很多时间
	- 避免：数据库的引擎支持的事务支持可重复读的隔离级别

### 表级锁

- 表锁
	- 读锁（表级别的共享锁）：`lock tables xxx read;`。允许当前会话读取被锁定的表，但阻止其他会话对这些表进行写操作。不能 进行写操作，不能访问其他表
	- 写锁（表级别的独占锁）：`lock tables xxx write`。允许当前会话对表进行读写操作，但阻止其他会话对这些表进行任何操作
- 元数据锁（MDL）
	- MDL读锁：CRUD操作
	- MDL写锁：结构变更操作
	- 事务执行期间，MDL是一直持有的
	- 申请MDL锁的操作会形成一个队列，队列中写锁获取优先级高于读锁。一旦出现MDL写锁等待，会阻塞后续该表的所有CRUD操作
		- 解决：对表结构变更前，先看数据库中的长事务，是否有事务已经对表加上MDL读锁，如果有，考虑kill掉长事务，再做表结构的变更
- 意向锁
	- 目的：快速判断表里是否有记录被加锁
	- 当执行插入、更新、删除操作，需要先对表加上【意向独占锁】，再对该记录加独占锁
	- 普通select语句是利用MVCC实现一致性读，不会加行级锁，是无锁的；但是可以对记录加共享锁和独占锁。
		```sql
		- -先在表上加上意向共享锁，然后对读取的记录加共享锁
		select ... lock in share mode;
		- -先在表上加上意向独占锁，然后对读取的记录加独占锁
		select ... for update;
		```
	- 意向锁是表级锁，不会和行级的共享锁和独占锁发生冲突，而且意向锁之间也不会发生冲突，只会和共享表锁（lock table ... read) 和独占表锁（lock table ... write) 发生冲突
	- 表锁和行锁满足读读共享、读写互斥、写写互斥
	- 如果没有意向锁，那么加独占表锁时，就需要遍历表里所有记录，查看是否有记录存在独占锁，这样效率会很慢
- AUTO-INC 锁
	- 特殊的表锁机制，插入数据时会加一个表级别的AUTO-INC锁，然后被AUTO_INCREMENT修饰的字段赋值递增的值，执行完插入语句后释放。事务在持有锁的过程中其他事务的插入语句会被阻塞。→大量数据插入时会影响插入性能
	- InnoDB 提供轻量级的锁来实现自增。插入数据时会被AUTO_INCREMENT修饰的字段加上轻量级锁，给该字段赋值一个自增的值，把轻量级锁释放，不需要等待整个插入语句执行完后才释放。
	- 当innodb_autonic_lock_mode = 2 （轻量级锁）并且 binlog_format = row ，既能提升并发性，又不会出现数据一致性问题

### 行级锁

普通select语句是利用MVCC实现一致性读，不会加行级锁，是无锁的；但是可以对记录加共享锁和独占锁。执行下面两条语句时之前要加上begin或者start transaction开启事务的语句
```sql
- -先在表上加上意向共享锁，然后对读取的记录加共享锁
select ... lock in share mode;
- -先在表上加上意向独占锁，然后对读取的记录加独占锁
select ... for update;
```

共享锁S锁（读读共享，读写互斥），独占锁X锁（写写互斥，读写互斥）
- **Record Lock，记录锁**：S锁和X锁。
	- 一个事务对一条记录加了S锁后，其他事务也可以对该记录加S锁，但不可以加X锁；
	- 一个事务对一条记录加了X锁后，其他事务不可以对该记录加S锁，也不可以加X锁；
- **Gap Lock，间隙锁**：存在于可重复读隔离级别和串行化隔离级别；
	- 目的：为了解决可重复读隔离级别下的幻读现象
	- 区间：前开后开
	- 锁之间兼容，两个事务可以同时持有包含共同间隙范围的间隙锁
- **Next-Key Lock**，上面两个锁的组合，锁定一个范围，并且锁定记录本身
	- 范围：前开后闭
	- 如果事务在插入记录时判断到插入位置已被其他事务加了间隙锁，插入操作就会发生阻塞，直到释放间隙锁为止，期间会生成一个**插入意向锁**。（不是意向锁，是一种特殊的间隙锁，属于行级别锁。锁住的是一个点。）然后锁的状态设置为等待状态
	- 在能使用记录锁或者间隙锁就能避免幻读现象的场景下，Next-Key Lock就会退化成记录锁或者间隙锁
	- 如果一个事务获取了X型的next-key锁（1,10]，另一个事务在获取相同范围的X型next-key锁时会被阻塞。因为是包含间隙锁和记录锁的，X型记录锁和X型记录锁是冲突的。但是范围到正无穷的next-key锁是可以同时持有的，不会冲突。

### MySQL是怎么加行级锁的？

查看事务执行过程中加了什么锁：`select * from performance_schema.data_locks\G;`
	通过LOCK_MODE确认锁类型
		x：next-key锁
		x，REC_NOT_GAP：记录锁
		x，GAP：间隙锁

- 唯一索引（主键索引）等值查询
	- 查询的记录存在，退化为记录锁
	- 不存在，在索引树找到第一条大于该查询记录的记录后，该记录的索引中的锁退化成间隙锁
- 唯一索引（主键索引）范围查询
	- 【大于】：Innodb存储引擎会用一个特殊的记录（supremum pseudo-record）来标识最后一条记录
		- select * from user where id > 15 for update;![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818212826425.png)

	- 【大于等于】，如果记录存在于表，退化为记录锁
		- select * from user where id >= 15 for update;![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818213056775.png)

	- 【小于或者小于等于】
		- 记录不在，扫描到终止范围查询的记录时，退化成间隙锁
		- 记录在
			- 【小于】：扫描到终止范围查询的记录时，退化成间隙锁，其他扫描到的记录都是在索引上加next-key锁
				- select * from user where id < 6 for update;![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818213322054.png)
				- select * from user where id < 5 for update; ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818213813354.png)


			- 【小于等于】：不会退化。其他扫描到的记录都是在索引上加next-key锁
				- select * from user where id <= 5 for update;
				- 
				- 以下是deepseek的分析：
					-  定位到id=1 → 加X型记录锁
					- 向右扫描到id=5 → 加X型记录锁
					- 继续扫描发现id=10不满足条件 → 加(5,10)间隙锁
					-  **不需要额外加(-∞,1)锁**，因为：
					    - 已锁定所有实际存在的≤5记录
					    - 新插入的id<1的记录自然也会≤5，但这类插入会被其他机制阻止
				- 当锁定一个索引记录时，InnoDB 会**自动包含该记录之前的间隙**
				- 例如锁定 id=5 时，实际锁定的是 (前一个id, 5] 区间
				- - 索引扫描过程中，InnoDB 会**动态评估需要保护的范围
				- 即使某些间隙没有显式锁定，系统也会保证这些区域不会被插入新记录
				- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818213547542.png)
				-  为什么不是(-∞,1]的next-key锁？
					
					- **效率考量**：从索引最左端(-∞)开始加锁会锁定过多不存在的范围
					    
					- **实际需要**：只需要确保扫描过程中不出现新的≤5的记录即可
					    
					- **实现机制**：InnoDB采用"懒加载"式加锁，只在必要时扩展锁范围

- 非唯一索引等值查询
	- 记录存在：对扫描到的二级索引记录加next-key锁，对于第一个不符合条件的二级索引记录，该二级索引的next-key会退化成间隙锁。同时在符合查询条件的记录的主键索引加上记录锁。直到扫描到第一个不符合条件的二级索引记录就停止扫描
		- select * from user where age = 22 for update；//age是二级索引，22存在![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818215909469.png)
		- next-key锁阻塞：age=21，id大于5；age=22，id小于10，id大于10（下一条记录有间隙锁）
		- X型间隙锁阻塞：age=39，id小于20；age=22，id小于10；age=22，id大于20；
		- 即无法插入任何age=22的语句，避免幻读发生
	- 记录不存在：扫描到第一条不符合条件的二级索引记录，退化成间隙锁。不会对主键索引加锁。（如果没有记录则是next-key锁，范围到右无穷，左开右闭）
		- select * from user where age = 25 for update；//age是二级索引，25不存在![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818214548072.png)
		- 插入语句是否会发生阻塞要看插入位置的下一条记录有没有间隙锁，有就会阻塞，没有就可以插入。（要根据主键索引排序）
			- age = 22， id=3：成功，因为下一位是山治
			- age=22，id=12：阻塞，因为下一位是香克斯，有间隙锁
		- LOCK DATA: 39, 20 + LOCK MODE : X, GAP的意思是，事务A在age=39记录的二级索引上(INDEX_NAME:index_age)，加了age值范围为(22,39)的X型间隙锁，**同时针对其他事务插入age值为39的新记录时,不允许插入的新记录的id值小于20** 。如果插入的新记录的id值大于20,则可以插入成功。
		- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818215820698.png)

- 非唯一索引范围查询
	- 索引的next-key Lock不会退化为间隙锁和记录锁
	- select * from user where age >= 22 for update;![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250818221041198.png)

- 没有加索引 的查询：update和delete语句如果查询条件不加索引那么就是全表扫描，会对每一条记录的索引加锁，相当于锁住了全表
	- ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250819115154388.png)
	- 避免：sql_safe_updates设置为1
		- update满足以下条件之一才能执行成功
			- 使用where，条件中必须有索引列；或者使用force index([index_name])告诉优化器使用哪个索引
			- 使用limit
			- 使用where和limit，此时where条件中可以没有索引列
		- delete
			- 同时使用where和limit，此时where条件中可以没有索引列

### 死锁

事务A在二级索引上加的是X型的next-key锁，范围是（1006，+]；
执行插入语句时会在插入间隙上获取插入意向锁，而**插入意向锁和间隙锁是冲突的**，所以当其他事务持有该间隙锁时，需要等待其他事务（检查1007和1008订单是否存在）释放间隙锁之后才能获取到插入意向锁。而间隙锁与间隙锁之间是兼容的，所以两个事务（检查1007和1008订单是否存在）中select update语句不会相互影响。
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250819140037537.png)

### 如何避免死锁？

死锁的**必要条件**：互斥、占有且等待、不可强占有、循环等待

- 设置事务等待锁的超时时间：等待时间超过设定值就对事务进行回滚，锁释放。Innodb_lock_wait_timeout
- 开启主动死锁检测：发现死锁后，主动回滚死锁链条中的某一个事务。innodb_deadlock_detect
### 隐式锁

事务需要加锁时，如果这个锁不可能发生冲突，Innodb会跳过加锁环节，这种机制称为隐式锁。
是Innodb实现的一种延迟加锁机制，特点是只有在可能发生冲突时才加锁，从而减少锁的数量，提高系统整体性能。
- 如果记录之间加有间隙锁，为了避免幻读，此时是不能插入记录的
- 如果Insert的记录和已有记录存在唯一键冲突，此时也不能插入记录
### Insert语句是怎么加行级锁的？

靠聚簇索引记录自带的trx_id 隐藏列来作为隐式锁来保护记录的

1. 记录之间加有间隙锁
2. 遇到唯一键冲突
	1. 如果主键索引重复，插入新纪录的事务会给已存在的主键值重复的聚簇索引记录添加**S型记录锁**
	2. 如果唯一二级索引重复，插入新纪录的事务会给已存在的二级索引列值重复的二级索引记录添加**S型next-key锁**

两个事务执行过程中，执行了相同的Insert语句，（唯一二级索引）
1. 事务A先插入1006记录，对应的唯一二级索引记录被【隐式锁】保护，还没有实际的锁结构
2. 事务B插入，遇到重复的唯一二级索引列值，想获取一个S型的next-key锁，但是事务A未提交，A插入的1006记录上的【隐式锁】会变【显示锁】且锁类型为X型的记录锁，所以事务B获取S型next-key锁时会遇到锁冲突，B进入阻塞状态
3. 如果不是唯一二级索引那么不会发生阻塞
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250819143146184.png)


### lc整理
- 计算字符串中字符数： CHAR_LENGTH(str)
- 时间计算函数
	- `TIMESTAMPDIFF`可以计算相差天数、小时、分钟和秒，相比于`datediff`函数要灵活很多。格式是时间小的前，时间大的放在后面。
		- WHERE TIMESTAMPDIFF(DAY, w2.recordDate, w1.recordDate) = 1
	- `adddate()`函数:将指定的时间间隔添加到日期值date是表示日期的值，它可以是 `String`、`DATE（YEAR、MONTH 和 DAY）`、`DATETIME（HOURS、MINUTES 或 SECONDS）`或 `TIMESTAMP` 类型。
		- **ADDDATE( date , INTERVAL value addunit )**  **ADDDATE( date , days )**
			- **date**：必填。要修改的日期
			- **days**: 必填。要添加到日期的天数
			- **value**：必填。要添加的时间/日期间隔的值。允许正值和负值
			- **addunit**：必填。要添加的间隔类型。可以是以下值之一：`MICROSECON` 、 `DSECOND`、`MINUTE`、`HOUR`、`DAY`、`WEEK`、`MONTH`、`QUARTER`、`YEAR`、 `SECOND_MICROSECOND`、`YEAR_MONTH`、`MINUTE_MICROSECOND`、`MINUTE_SECOND` 、`HOUR_MICROSECOND`、`HOUR_SECOND` 、`HOUR_MINUTE`、`DAY_MICROSECOND`、`DAY_SECOND`、`DAY_MINUTE`、`DAY_HOUR`
			- a.recorddate = adddate(b.recorddate,INTERVAL 1 day)
- `round(avg(t2.timestamp - t1.timestamp), 3)` 是用于计算时间差平均值并四舍五入的SQL函数组合，具体含义如下：
	1. `t2.timestamp - t1.timestamp`：计算两个时间戳（t2和t1）之间的时间差
	2. `avg(...)`：计算所有这些时间差的平均值
	3. `round(..., 3)`：将平均值四舍五入到3位小数
	整体意思是：计算t2和t1时间戳差的平均值，并将结果保留3位小数。
- cross join
- group by：分组计算查询。按group by中的组别进行计算。
- order by
- ifnull(条件，0)：如果条件成立则返回条件值，否则返回0。作用：不显示null，将null值转为0
- ` round(ifnull(avg(c.action = 'confirmed'),0),2)`
	1. `c.action = 'confirmed'`是一个布尔表达式：当action 值为 'confirmed' 时，返回1，否则返回0
	2. `AVG()` 函数会对这些 1 和 0 计算平均值
	3. 实际上计算的是 **'confirmed' 记录所占的比例**
	4. 等价写法：avg(c.action = 'confirmed')
		1. COUNT(CASE WHEN c.action = 'confirmed' THEN 1 END) * 1.0 / COUNT(* )
		2. sum(if(action = 'confirmed',1,0))/count(action)
	5. ifnull(···，0)： 如果某个用户没有任何确认记录（即 `AVG` 返回 `NULL`），则返回0作为默认值。
	6. round( ··· ,2)：保留两位小数