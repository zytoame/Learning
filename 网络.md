
## HTTP
### TCP/IP网络模型
TCP/IP网络：应用层，传输层，网络层，网络接口层
	应用层：支持HTTP、SMTP等最终用户进程
	传输层：处理主机到主机的通信（TCP、UDP）
	网络层：寻址和路由数据包（IP协议）
	链路层：通过网络的物理电线电缆或无限信道移动比特
	![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250730171752222.png)
传输单位：可以统称数据包
	网络接口层：帧（frame）
	网络层：包（packet）
	TCP层：段（segment）
	HTTP：消息/报文（message）

	MTU：一个网络包的最大长度（IP头部+TCP头部+数据），以太网中一般为1500字节
	MSS：除去IP头部和TCP头部之后，一个网络包所能容纳的TCP数据的最大长度

**==TCP和UDP区别==**
- **连接**：TCP是面向连接的传输协议，传输数据前先要建立连接；UDP不需要连接，即刻传输数据
- **服务对象**：TCP是一对一的两点服务，即一条连接只有两个端点；UDP支持一对一，一对多，多对多的交互通信
- **可靠性**：TCP是可靠交付数据，数据可以无差错、不丢失、不重复、按序到达；UDP是尽最大努力交付，不保证可靠交付数据。但是可以基于UDP传输协议实现一个可靠的传输协议，比如QUIC协议
- **拥塞控制、流量控制**：TCP有拥塞控制和流量控制机制，保证数据传输的安全性；UDP则没有，即使网络非常拥堵，也不会影响UDP的发送速率
- **首部开销**：TCP首部长度较长，会有一定的开销，首部在没有使用”选项“字段时是20个字节，如果使用”选项“字段则会变长。UDP首部只有8个字节，并且是固定不变的，开销较小。
- **传输方式**：TCP是流式传输，没有边界，但保证顺序和可靠。UDP是一个包一个包的发送，有边界，但可能会丢包和乱序
  

IP地址
	网络号：ip地址 和      子网掩码        进行 按位与运算AND 
	主机号：ip地址 和 子网掩码取反后  进行 按位与运算AND 
	![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250730170227803.png)

寻址过程
1. IP 头部中的接收方 IP 地址表示网络包的目的地
2. 路由：找到目标地址的子网，IP地址的网络号进行转发，将数据包转发到对应的网络



### 键入网址到网页显示的过程

1. HTTP：浏览器做的第一步是解析URL，确定web服务器和文件名，生成发送给web服务器的HTTP请求信息
	![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250801222144465.png)

2. DNS：查询服务器域名对应的IP地址
	-  例如：www.server.com.
		1. 根DNS服务器（.）信息保存在互联网中所有的DNS服务器中
		2. 顶级域DNS服务器（.com）
		3. 权威DNS服务器（server.com）
	1. 浏览器先查看自身有无缓存
		1. 操作系统有无缓存
			1. hosts文件有无缓存
				1. 本地DNS服务器
	2. 客户端发送一个DNS请求，询问www.server.com的IP，并发送给本地DNS服务器（客户端的TCP/IP设置中填写的DNS服务器地址）
	3. 本地DNS收到请求后，先去缓存中查找www.server.com，如果能够找到则直接返回IP地址；不能则询问根域名服务器
	4. 根域名DNS服务器收到请求后，发现后置是（.com），返回顶级域DNS服务器地址
	5. 本地DNS收到后，向顶级域DNS服务器发起请求询问IP地址
	6. 顶级域服务器返回权威DNS服务器的地址
	7. 本地DNS收到后，向权威DNS服务器发起请求询问IP地址
	8. 权威DNS服务器查询后将IP地址返回给本地DNS
	9. 本地DNS再将IP地址返回给客户端，客户端和目标建立连接

3. 协议栈：获取到IP之后，应用程序（浏览器）通过调用socket库，把HTTP的传输工作委托给操作系统的协议栈
	-  协议栈组成
		1. 上半部分：TCP和UDP协议，负责收发数据，接受应用层的委托执行收发数据的操作
			1. **==TCP==**：三次握手
				1. 源端口号、
				2. 目标端口号，
				3. 序号：解决包乱序问题
				4. 确认序列：确认发出去对方是否收到，没有收到重新发送直到送达。解决丢包问题
				5. 状态位：带状态位的包的发送会引起双方状态变更。SYN（发起连接），ACK（回复），RST（重新连接），FIN（结束连接）
				6. 窗口大小：流量控制。通信双方各声明一个窗口（缓存大小），标识自己当前能够的处理能力
				7. 拥塞控制：控制发送的速度
			2. UDP
		2. 下半部分：IP：用IP协议控制网络包收发操作
			1. ICMP协议：告知网络包传送过程中产生的错误以及各种控制信息
			2. ARP协议：根据IP地址查询想要的以太网MAC地址
			- IP协议包含：源地址IP（客户端输出的IP地址），目标地址IP（通过DNS域名解析得到的Web服务器IP）
				- IP包头的协议号：06（十六进制），表示协议为TCP

	1. 传输数据之前，TCP建立连接：**==三次握手：保证双方都有发送和接受的能力==**
		- 客户端和服务端处于closed状态。
		- 服务端主动监听某个端口，处于listen状态
		- 客户端主动发起连接SYN，处于SYN-SENT状态
		- 服务端收到发起的连接，返回SYN，并且ACK客户端的SYN，处于SYN-RCVD状态
		- 客户端收到SYN和ACK之后，发送对SYN确认的ACK，处于established状态，因为一发一收成功了
		- 服务端收到ACK的ACK之后，处于established状态
		- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250801224949041.png)
	2. 组装TCP报文：TCP头部+数据（HTTP头部+数据）
	3. TCP模块委托IP模块将数据封装成网络包发送给通信对象：通过路由表规则判断，选择其中一个网卡作为源地址IP，将包发送给路由器
		1. IP头部源地址 的选择：根据路由表规则判断
		2. 将Web服务器的目标地址和第一条目的子网掩码（Genmask）进行与运算，将结果与条目1的destination进行比较；
		3. 匹配失败对第二条目的进行运算比较，匹配成功则使用此网卡的IP地址作为IP包头的 源地址；
		4. 第三条目表示默认网关，如果其他条目都无法匹配，就会自动匹配这一行。后续把包发给路由器（Gateway为路由器的IP地址）
		5. ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250802125027605.png)
	4. 生成IP头部之后，需要在前面加上MAC头部
		- MAC头部：以太网使用的头部，包含接收方和发送方的MAC地址等信息，用于两点之间的传输
		- 协议类型
			- 0800：IP协议
			- 0806：ARP协议
		1. 获取发送方的MAC地址：读取网卡的ROM
		2. 获取接收方的MAC地址
			1. 查询ARP缓存，如果已保存对方的MAC地址，则不需要发送ARP查询
			2. ARP协议在以太网中以广播的形式进行查询
			3. 如果两方处于同一子网中，就可以得到MAC地址。如果不是，则把数据包转发给路由器知道目标主机的路由器。
			4. 后续操作系统将查询结果存入ARP缓存的内存空间中，TTL时间为几分钟
4. 网卡：将数字信息转换为电信号，在网线上传输。通过网卡驱动程序控制网卡
	1. 网卡驱动获取网路包之后，将其复制到网卡内的缓存区，在其开头加上报头和起始帧分界符，在末尾加上用于检测错误的帧校验序列（FCS）
5. 交换机：
	- **交换机**是基于以太网设计的，俗称**二层**网络设备，交换机的端口不具有 MAC 地址。
	1. 电信号到达网线接口，交换机的模块进行接受，将电信号转换为数字信号。通过FCS校验错误，没问题则放入缓冲区
	2. 根据MAC地址表查询MAC地址，将信号发送到相应的端口，如果查询不到，或者接收方MAC地址为广播地址，则发送到出来源端口之外的所有端口上
		1. MAC地址表包含设备的MAC地址，以及，设备和交换机连接的端口
		2. 广播地址：
			1. MAC地址：FF:FF:FF:FF:FF:FF
			2. IP地址：255.255.255.255
6. 路由器
	- **路由器**是基于 IP 设计的，俗称**三层**网络设备，路由器的各个端口都具有 MAC 地址和 IP 地址；
	1. 路由器端口接收发给自己的以太网包：电信号到达网线接口，路由器的模块进行接受，将电信号转换为数字信号。通过FCS校验错误，没问题则检查MAC头部中的接收方MAC地址，确认是否发给自己，是则放入缓冲区，否则丢弃。
	2. 丢弃 包开头的MAC头部
	3. 根据MAC头部后方的IP头部进行转发，查询路由表确定输出端口
		1. 查询路由表判断转发目标
		2. 根据路由表的网关列判断对方的地址：如果网关是IP地址则为目标地址，说明未抵达终点，需要路由器继续转发；如果网关为空，说明已抵达终点，得到对方的IP地址
		3. 通过ARP协议查询接收方的MAC地址，先查缓存再广播查询
		4. 发送方MAC地址：输出端口的MAC地址+以太类型字段（0800表示IP协议）
	4. 转换为电信号通过端口发送，到达下一个路由器，到达最终服务器
7. 服务器解包： MAC → IP → TCP（序列包） → HTTP（监听端口，发送给进程） →封装到HTTP响应报文 
8. → 套上TCP、IP、MAC头部（源地址是服务器IP地址，目的地为客户端IP地址）→ 网卡 → 路由器 → 交换机 → 
9. 客户端 解包 →
10. 浏览器渲染，显示
11. 客户端向服务器发起**==TCP四次挥手==**
	1. 客户端发送 `FIN=1`（Finish）报文，并进入 `FIN_WAIT_1` 状态。表示客户端没有数据要发送了，但还能接收数据
	2. 服务器收到 `FIN` 后，发送 `ACK` 确认报文，进入 `CLOSE_WAIT` 状态。- 客户端收到 `ACK` 后进入 `FIN_WAIT_2` 状态。- 此时服务器可能还有未发送完的数据（半关闭状态）。
	3. 服务器完成数据发送后，发送 `FIN=1` 报文，进入 `LAST_ACK` 状态；- 表示服务器也准备关闭连接。
	4. - 客户端收到 `FIN` 后，发送 `ACK` 确认报文，进入 `TIME_WAIT` 状态（等待 `2MSL` 时间）。- 服务器收到 `ACK` 后立即关闭连接。- 客户端等待 `2MSL` 后也关闭连接。
	5. ![i](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809150121180.png)

### linux系统收发网络包

- OSI网络模型（开放式系统互联通信参考模型）7
	- 应用层：给应用程序提供同一接口
	- 表示层：把数据转换成兼容另一个系统能识别的格式
	- 会话层：建立、管理和终止表示层实体之间的通信会话
	- 传输层：端到端的数据传输
	- 网络层：数据的路由、转发、分片
	- 数据链路层：数据的封帧、差错检测，MAC寻址
	- 物理层：物理网络中传输数据帧
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250802142627467.png)
- linux网络协议栈
	- ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250802142726450.png)

- 接受网络包
	1. **网卡**接受到网络包之后通过**DMA技术**将包写入指定的内存地址**Ring Buffer**（环形缓冲区）
	2. 网卡CPU发起**硬件中断**，**CPU**收到中断请求，根据中断表调用已经注册的中断处理函数
	3. 硬件中断处理函数会先“暂时屏蔽中断”，表示已经知道内存中有数据，告诉网卡下次再收到数据包直接写内存就可以了，不要再通知CPU，提高效率，避免不停被中断。
	4. 发起“软中断”，恢复刚才屏蔽的中断
	5. **ksoftirqd内核线程**收到软中断后，轮询处理数据：从Ring Buffer中获取一个数据帧，用sk_buff表示，从而可以作为一个网络包交给网络协议栈进行逐层处理
	6. **网络接口层**：检查报文的合法性：不合法丢弃；合法找出上层协议的类型（IPv4/IPv6），去掉帧头帧尾，交给网络层
	7. **网络层**：取出IP包，判断网络包下一步走向。发给本机：IP头中上一层协议的类型（TCP/UDP），去掉IP头
	8. **传输层**：取出TCP头或UDP头，根据四元组“源，源端口，目的IP，目的端口”，找出对应的socket，并把数据放到socket的接收缓冲区
	9. **应用层**：调用socket接口，将内核的socket接收缓冲区的数据“拷贝” 到应用层的缓冲区，唤醒用户进程
	10. ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809120545700.png)

	- 最简单：触发一个中断告诉操作系统网络包已到达
		- 问题：高性能场景下，网络包数量非常多，触发的中断非常多，导致CPU一直处理中断导致其他任务无法继续，影响整体效率
	- NAPI机制：混合“中断和轮询”，采用中断唤醒数据接收的服务程序，然后poll的方法来轮询数据

- 发送网络包
	1. **应用程序**：调用socket发送数据包的接口。从用户态陷入到内核态中的socket层，内核会申请一个内核态的sk_buff 内存，将用户待发送的数据拷贝到sk_buff 内存，将其加入到发送缓冲区
	2. socket层：网络协议栈：从socket发送缓冲器中取出sk_buff，并按照TCP/IP协议栈从上到下逐层处理
	3. **传输层**：if：TCP传输协议发送数据：拷贝一个新的sk_buff 副本，对sk_buff （各个层的数据包）填充TCP头
		- 因为sk_buff 后续在调用网络层，最终到达网卡发送完成时会被释放掉。而TCP协议支持丢失重传，在收到对方的ACK之前，这个sk_buff 不能被删除。
		- 所以，内核的做法是 调用网卡发送时，传递出去的是sk_buff 的一个拷贝，等收到ACK再真正删除
		1. 全部数据包只用**一个结构体**来描述。通过调整sk_buff 中的**data指针**来实现
		2. 接受报文时，从网卡驱动开始，通过协议栈层层往上传送数据报，通过增加skb->data 的值，来逐步剥离协议首部
		3. 发送报文：创建sk_buff 结构体，数据缓存区的头部预留足够的空间，用来填充各层首部，在经过各下层协议时，通过减少skb->data 的值来增加协议首部
		4. **发送方**必须保留已发送数据的副本（存储在 `sk_buff` 中），直到收到对方的 **ACK 确认**。
		    
		- **接收方**收到该报文后，会回复一个 **ACK 报文**（携带确认号，表示已成功接收）。
		    
		- 只有收到 ACK 后，发送方才能安全释放对应的 `sk_buff` 内存。
		1. 发送报文的过程![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809151400296.png)
	4. 网络层：选取路由（确认下一跳的IP），填充IP头，netfilter过滤，对超过MTU大小的数据包进行分片
	5. 网络接口层：通过ARP 协议获取下一跳的MAC地址，对sk_buff 填充帧头帧尾，将skb放到网卡的发送队列中
	6. 触发“软中断”告诉网卡驱动程序有新的网络包需要发送，驱动程序从发送队列中读取sk_buff ，将其挂到RingBuffer中，加工skbuff数据映射到网卡可访问的内存DMA区域，触发真实的发送
	7. 发送完成后，网卡设备触发一个硬中断来释放临时内存（DMA缓冲区/ sk_buff 和 RingBuffer）
	8. 当收到这个TCP报文的ACK应答时，传输层就会释放原始sk_buff
		
		假设客户端发送一个 HTTP 请求 `GET /index.html`：
		
		2. 内核构造 TCP 报文：
		    
		    - 序列号（Seq）= 1000（假设初始值）。
		        
		    - 数据长度 = 20 字节（`GET /index.html`）。
		        
		3. 网卡发送该报文（Seq=1000, Len=20）。
		    
		4. 服务器收到后回复 `ACK=1021`（即 `1000 + 20 + 1`，TCP 的确认号是期望收到的下一个字节序号）。
		    
		5. 客户端收到 `ACK=1021` 后，释放序列号 `1000-1020` 对应的原始 `sk_buff`。
		    
		
		**这里的“这个 TCP 报文”**就是客户端发送的 `Seq=1000, Len=20` 的报文。
		
	- 涉及三次内存拷贝
		1. 将用户待发送的数据拷贝到sk_buff 内存中
		2. 克隆每个sk_buff ，生成副本skbuff
		3. 当skbuff大于MTU时申请额外的skbuff，将原来的skbuff拷贝为多个小的skbuff

## 一、Linux 常用命令（核心篇）

| 命令 | 功能描述 | 常用示例 |
| :--- | :--- | :--- |
| `ls` | 列出目录内容 | `ls -l` (详细列表), `ls -a` (包括隐藏文件) |
| `cd` | 切换目录 | `cd /home`, `cd ..` (上一级) |
| `pwd` | 显示当前工作目录的绝对路径 | `pwd` |
| `mkdir` | 创建新目录 | `mkdir new_folder` |
| `rm` | 删除文件或目录 | `rm file.txt`, `rm -r folder` (递归删除目录) |
| `cp` | 复制文件或目录 | `cp source.txt dest.txt`, `cp -r dir1 dir2` |
| `mv` | 移动或重命名文件/目录 | `mv old.txt new.txt`, `mv file /tmp/` |
| `cat` | 查看文件内容 | `cat file.txt` |
| `less` / `more` | 分页查看文件内容 | `less long_file.log` |
| `head` / `tail` | 查看文件头部/尾部内容 | `tail -f logfile` (实时追踪日志) |
| `grep` | 文本搜索 | `grep "error" logfile.txt` |
| `find` | 查找文件 | `find /home -name "*.txt"` |
| `ps` | 查看进程状态 | `ps aux` |
| `top` / `htop` | 动态查看进程和系统资源 | `top` |
| `kill` | 终止进程 | `kill 1234`, `kill -9 1234` (强制终止) |
| `chmod` | **改变文件/目录的权限** | `chmod 755 script.sh` |
| `chown` | 改变文件/目录的所有者 | `chown user:group file.txt` |
| `df` | 查看磁盘空间使用情况 | `df -h` (人类可读格式) |
| `du` | 查看目录/文件的磁盘使用量 | `du -sh /home` |
| `tar` | 打包和解压文件 | `tar -czvf archive.tar.gz folder/`, `tar -xzvf archive.tar.gz` |

---

## 二、Linux 权限设置命令

Linux 中文件和目录的权限主要通过 `chmod` (change mode) 命令来设置。

### 1. 权限的表示法
权限分为三种身份：
- **u** (user/owner): 文件所有者
- **g** (group): 所属用户组
- **o** (others): 其他用户
- **a** (all): 所有用户 (即 u+g+o)

每种身份有三种基本权限：
- **r** (read): 读权限 (数字表示为 4)
- **w** (write): 写权限 (数字表示为 2)
- **x** (execute): 执行权限 (数字表示为 1)

### 2. `chmod` 命令的两种用法

#### 方法一：符号模式 (相对直观)
**语法：** `chmod [身份][操作符][权限] 文件`

| 操作符 | 含义 |
| :--- | :--- |
| `+` | 添加权限 |
| `-` | 移除权限 |
| `=` | 设置精确权限 |

**示例：**
- `chmod u+x script.sh` -> 给**所有者**添加**执行**权限。
- `chmod go-w file.txt` -> 移除**组**和**其他用户**的**写**权限。
- `chmod a=rw config.conf` -> 给**所有用户**设置**读和写**权限（无执行）。

#### 方法二：数字模式 (绝对模式，更常用)
**语法：** `chmod [ABC] 文件`
- **A** 代表**所有者 (u)** 的权限数字之和
- **B** 代表**用户组 (g)** 的权限数字之和
- **C** 代表**其他用户 (o)** 的权限数字之和

**权限数字计算：**
- 读 (r) = 4
- 写 (w) = 2
- 执行 (x) = 1
- 无权限 = 0

**组合示例：**
- 读写权限 (rw-)：4 (读) + 2 (写) + 0 (无执行) = `6`
- 读写执行权限 (rwx)：4 + 2 + 1 = `7`
- 只读权限 (r--)：4 + 0 + 0 = `4`

**常用数字权限示例：**
- `chmod 755 file` -> **rwxr-xr-x** (所有者可读/写/执行，组和其他可读/执行)
- `chmod 644 file` -> **rw-r--r--** (所有者可读/写，组和其他只读)
- `chmod 600 file` -> **rw-------** (只有所有者可读/写，非常私有)

---

## 三、详解 `777` 权限

`chmod 777` 是权限设置的**极端情况**，需要谨慎使用。

### 1. 它代表什么？
数字 `777` 是三个数字的组合：
- 第一个 `7`: **所有者 (u)** 拥有 `4+2+1`，即 **读(r)、写(w)、执行(x)** 权限。
- 第二个 `7`: **所属组 (g)** 拥有 `4+2+1`，即 **读(r)、写(w)、执行(x)** 权限。
- 第三个 `7`: **其他用户 (o)** 拥有 `4+2+1`，即 **读(r)、写(w)、执行(x)** 权限。

**结论：** `chmod 777` 意味着**系统上的任何用户**都可以**读取、修改、删除**这个文件或目录，或者**执行**这个程序。

### 2. 何时使用？
- **临时调试**：当遇到权限错误，为了快速排除是否是权限问题。
- **极度不重要的文件**：例如 `/tmp` 目录下的临时文件。
- **在完全信任的封闭环境中**。

### 3. 为什么强烈不建议使用？
- **极大的安全风险**：任何用户（包括黑客或恶意软件）都可以修改或删除该文件。
- **不符合最小权限原则**：最佳实践是只授予完成工作所必需的最小权限。

### 4. 更安全的替代方案
绝大多数情况下，你应该使用更严格的权限：
- 对于**可执行脚本/程序**：`755` (`rwxr-xr-x`)
- 对于**配置文件**（仅需所有者修改）：`644` (`rw-r--r--`) 或 `600` (`rw-------`)
- 对于**私有数据目录**：`700` (`rwx------`)

---

### 总结
- **常用命令**是操作 Linux 的基础。
- **`chmod`** 是设置权限的核心命令，掌握**数字模式**更高效。
- **`777`** 意味着对所有人完全开放，**方便但极其危险**，应避免在生产环境或重要文件上使用。

如果需要我解释某个特定命令的更多用法，或者你在笔记中 (`@vault`) 有相关的 Linux 学习内容，可以告诉我！

## 面试

- 目录
	- ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809154724593.png)

### HTTP是什么？

**超文本传输协议（HyperText Transfer Protocol）**
==HTTP是 一个在计算机世界里专门在**两点**之间**传输**文字、图片、音频、视频等**超文本**数据的**约定和规范**==

 HTTP使用计算机能够理解的语言确立了一种计算机之间交流通信的规范（两个以上的参与者），以及相关的各种控制和错误处理方法，专门在两点之间传输数据的（行为约定和规范）
HTTP是双向协议
HTTP传输超越普通文本的文字

### HTTP有哪些常见的状态码

1xx 类状态码属于提示信息，是协议处理中的一种中间状态，实际用到的比较少。
2xx 类状态码表示服务器成功处理了客户端的请求，也是我们最愿意看到的状态。
	「200 OK」是最常见的成功状态码，表示一切正常。如果是非 HEAD 请求，服务器返回的响应头都会有 body 数据。
	「204 No Content」也是常见的成功状态码，与 200 OK 基本相同，但响应头没有 body 数据。
	「206 Partial Content」是应用于 HTTP 分块下载或断点续传，表示响应返回的 body 数据并不是资源的全部，而是其中的一部分，也是服务器处理成功的状态。
3xx 类状态码表示客户端请求的资源发生了变动，需要客户端用新的 URL 重新发送请求获取资源，也就是重定向。
	「301 Moved Permanently」表示永久重定向，说明请求的资源已经不存在了，需改用新的 URL 再次访问。
	「302 Found」表示临时重定向，说明请求的资源还在，但暂时需要用另一个 URL 来访问。301 和 302 都会在响应头里使用字段 Location，指明后续要跳转的 URL，浏览器会自动重定向新的 URL。
	「304 Not Modified」不具有跳转的含义，表示资源未修改，重定向已存在的缓冲文件，也称缓存重定向，也就是告诉客户端可以继续使用缓存资源，用于缓存控制。
4xx 类状态码表示客户端发送的报文有误，服务器无法处理，也就是错误码的含义。
	「400 Bad Request」表示客户端请求的报文有错误，但只是个笼统的错误。
	「403 Forbidden」表示服务器禁止访问资源，并不是客户端的请求出错。
	「404 Not Found」表示请求的资源在服务器上不存在或未找到，所以无法提供给客户端。
5xx 类状态码表示客户端请求报文正确，但是服务器处理时内部发生了错误，属于服务器端的错误码。
	「500 Internal Server Error」与 400 类型，是个笼统通用的错误码，服务器发生了什么错误，我们并不知道。
	「501 Not Implemented」表示客户端请求的功能还不支持，类似“即将开业，敬请期待”的意思。
	「502 Bad Gateway」通常是服务器作为网关或代理时返回的错误码，表示服务器自身工作正常，访问后端服务器发生了错误
	「503 Service Unavailable」表示服务器当前很忙，暂时无法响应客户端

### HTTP有哪些常见字段

- Host字段：客户端发送请求，用来指定服务器的域名。`Host：www.A.com`
- Content-length：服务器在返回数据时，表明本次回应的数据长度
- Connection：最常用：客户端要求服务器使用【HTTP长连接】机制。`Connection:Keep-Alive`
- Content-Type：服务器回应时告诉客户端数据的格式
	- ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809162853477.png)
- Content-Encoding：数据的压缩方法
	- ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809162933607.png)

### GET和POST的区别？ GET和POST方法都是安全和幂等的吗

**GET**：从服务器获取指定的资源，**只读操作，安全且幂等**。可以对GET请求的数据做**缓存**，可以保存为书签。  但是用GET实现**新增或删除数据的请求不是安全幂等**的

**POST**：根据请求负荷（报文body）**对指定的资源做处理。不安全不幂等**。**不会缓存POST请求**也不能保存为书签。 但是使用POST实现**查询数据时是安全幂等**的

安全：请求方法不会破坏服务器上的资源
幂等：多次执行相同的操作，结果都是相同的

![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809163218139.png)
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809163246165.png)


### HTTP缓存有哪些实现方式？什么是强制缓存？什么是协商缓存？

HTTP缓存有两种实现方式：强制缓存和协商缓存
协商缓存的两个字段都需要配合强制缓存中的Cache-Control字段来使用，只有在未能命中强制缓存的时候才能发起带有协商缓存字段的请求

![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809170830578.png)

- 强制缓存
	
	只要浏览器判断缓存没有过期，直接使用浏览器的本地缓存。浏览器决定是否使用缓存
	利用HTTP响应头部字段实现，表示资源在客户端缓存的有效期
		Expires：绝对时间
		Cache-Control：相对时间，优先级高，选项更多，设置更精细，建议使用
			浏览器第一次访问服务器资源时，服务器会在返回这个资源的同时，在Response头部加上Cache-Control，其中设置了过期时间大小
			浏览器再次访问该资源时，通过请求资源的时间与Cache-Control中设置的过期时间大小，计算该资源是否过期。没有过期则使用该缓存，否则重新请求服务器
	
- 协商缓存
	与服务端协商之后通过协商结果来判断是否使用本地缓存
		
	基于两种头部实现
	- 请求头部：If-Modified-Since，响应头部：Last-Modified；基于时间实现
		- 当资源过期发现响应头中有Last-Modified声明（最后修改时间），再次发起请求的时候带上LM，
		- 服务器收到请求后发现有IMS，则与LM对比，如果修改时间较新说明被修改，返回最新资源，HTTP 200 OK；如果资源无新修改，响应HTTP 304走缓存
	- 请求头部：If-None-Match；响应头部：ETag（唯一标识响应资源）；基于一个唯一标识实现。ETag优先级高于Last-Modified
		- 资源过期-> 浏览器发现响应头中有ETag，再次向服务器发起请求，将请求头INM值设置为ETag的值。
		- 服务器收到请求后进行比对，如果资源没变化返回304，变化返回200.

### 为什么ETag的优先级更高？
	
1. 没有修改文件内容的情况下文件的最后修改时间可能会改变，导致客户端认为改变 ，重新请求
2. If-Modified-Since能检查到的粒度是秒级的，而有些文件在秒级以内修改的，使用ETag能够保证这种需求下客户端在一秒内能刷新多次
3. 有些服务器不能精确获取文件的最后修改时间

### HTTP/1.1 的优点有哪些

- 简单：基本报文格式为header+body，头部信息也是key-value简单文本的形式，易于理解
- 灵活和易于扩展：协议里的各类请求方法、URI/URL、状态码、头字段等每个组成要求都允许开发人员自定义和扩充；HTTP工作在应用层，下层可以随意变化
- 应用广泛和跨平台

### HTTP/1.1 的缺点有哪些

- 无状态双刃剑：服务器不会去记忆HTTP状态，无需额外资源来记录状态信息，减轻服务器负担；完成关联性操作时都要验证信息。如：无法进入需要登录验证的页面（登录后无法查看我的订单），个性化设置无法被保存，
	- 解决：cookie
	- ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809173346674.png)
- 明文传输双刃剑：传输过程中的信息是可方便阅读的，信息裸奔
- 不安全：
	- 通信使用明文（不加密），内容可能被窃听。如：账号信息泄露
	- 不验证通信方的身份，可能遭遇伪装。访问假的网站付款
	- 无法证明报文的完整性，可能遭遇篡改。广告植入

### HTTP/1.1的性能如何

HTTP协议基于TCP/IP，使用请求-应答的通信模式
- **长连接**：只要任意一端没有明确提出断开连接，则保持TCP连接状态
- **管道网络传输**：在同一个TCP连接里客户端发起多个请求，减少整体响应时间。服务器按照接受请求的顺序发送对这些管道化请求的相应。如果服务端在处理A请求时耗时较长，那么后续的请求的处理会被阻塞，称为队头阻塞。

性能瓶颈：
- 队头阻塞
HTTP/1.1管道解决了请求的队头阻塞，但是没有解决响应的队头阻塞
- 请求/响应头部冗长，未经压缩发送，首部信息越多延迟越大只能压缩body部分
- 没有请求优先级控制
- 请求只能从客户端开始，服务器只能被动响应
浏览器基本没有支持管道化技术

### HTTP/1.1如何优化？

- 尽量避免发送HTTP请求：缓存
- 在需要发送HTTP请求时，考虑如何减少请求次数
	- 减少重定向请求次数：由代理服务器完成重定向工作
	- 合并请求：为了防止单个请求的阻塞，所以一般浏览器会同时发起 5-6 个请求，每一个请求都是不同的 TCP 连接，那么如果合并了请求，也就会减少 TCP 连接的数量，因而省去了 TCP 握手和慢启动过程耗费的时间。合并资源，以大资源代替多个小资源。当大资源中的某个小资源发生变化后，客户端需要下载整个大资源文件，带来额外的网络消耗
	- 延迟发送请求：按需获取
- 减少服务器的HTTP响应的数据大小：压缩
	- 无损压缩：gzip
	- 有损压缩：WebP

### HTTP/2做了什么优化？

相比1.1的改进
- **头部压缩**：HPACK算法压缩（静态字典（Index：0~61），动态字典（Index从62开始；前提：在同一连接上传输完全相同的HTTP头部），Huffman编码（压缩算法））
	- ![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250811135756893.png)
	- 动态表具有时效性，如果第一次请求的时候发生丢包情况，之后的请求就无法解码hpack头部，会发生阻塞，直到首次数据包重传

- **二进制格式**
	- 帧头（Frame Header）只有9个字节，前3个字节是长度。有10中帧类型（数据帧，控制帧）；流标识符（Steam ID）![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250811135919557.png)

- **并发传输**：针对不同的 HTTP 请求用独一无二的 Stream ID 来区分，**接收端可以通过 Stream ID 有序组装成 HTTP 消息**，**不同 Stream 的帧是可以乱序发送的**，因此可以并发不同的 Stream ，也就是 HTTP/2 可以**并行交错地发送请求和响应**。
- **服务器主动推送资源**：客户端和服务器**双方都可以建立stream**，Stream ID客奇服偶

缺陷：
- **队头阻塞**：HTTP/2 是基于 TCP 协议来传输数据的，TCP 是字节流协议，TCP 层必须保证收到的字节数据是完整且连续的，这样内核才会将缓冲区里的数据返回给 HTTP 应用，那么**当「前 1 个字节数据」没有到达时，后收到的字节数据只能存放在内核缓冲区里**，只有等到这 1 个字节数据到达时，HTTP/2 应用层才能从内核中拿到数据，这就是 HTTP/2 队头阻塞问题。
	- 一旦发生了丢包现象，就会触发 TCP 的重传机制，这样在一个 TCP 连接中的所有的 HTTP 请求都必须等待这个丢了的包被重传回来。
- **TCP与TLS握手存在延迟：TCP三次握手和TLS四次握手，3个RTT时延**
	### 1. TCP 三次握手 (Three-Way Handshake)
	**目的**：在客户端（Client）和服务器（Server）之间建立一条可靠的连接。确保双方都具有发送和接收数据的能力。
	
	**过程与时延**：
	整个过程需要 **1.5 个 RTT（Round-Trip Time，往返时间）**。
	
	1.  **SYN (Synchronize)**：
	    *   **动作**：客户端发送一个 SYN 包（其中包含一个随机序列号 `seq=x`）到服务器，请求建立连接。
	    *   **时延**：**0.5 RTT**。客户端等待服务器回复，消耗了半个往返时间。
	2.  **SYN-ACK (Synchronize-Acknowledge)**：
	    *   **动作**：服务器收到 SYN 后，回复一个 SYN-ACK 包（包含自己的序列号 `seq=y` 和对客户端序列号的确认 `ack=x+1`）。
	    *   **时延**：**又一个 0.5 RTT**。这个包到达客户端又消耗了半个往返时间。至此，总耗时 **1 RTT**。客户端此时已确认自己和服务器的**发送、接收能力**都正常。
	3.  **ACK (Acknowledge)**：
	    *   **动作**：客户端收到 SYN-ACK 后，再发送一个 ACK 包（`ack=y+1`）给服务器。
	    *   **时延**：**“0” RTT**。客户端在发送完这个 ACK 后**立即可以开始发送应用数据**（例如 HTTP 请求）。这个 ACK 包本身去往服务器的过程不需要客户端等待，所以不产生额外的等待时延。
	
	**总结**：从开始到客户端可以发送数据，总时延为 **1 RTT**。服务器在收到第三个 ACK 包后连接才完全建立，此时它才能开始发送数据。
	
	### 2. TLS 握手 (以 TLS 1.2 为例)
	
	**目的**：在已建立的 TCP 连接之上，协商加密套件、验证服务器身份、生成会话密钥，以建立一条安全的加密通道。
	
	**过程与时延**：
	典型的 TLS 1.2 握手（基于 RSA 密钥交换）需要 **2 个 RTT**。
	
	4.  **Client Hello**：
	    *   **动作**：客户端发送支持的 TLS 版本、加密套件列表、一个随机数（`Client Random`）。
	    *   **时延**：客户端发送出去后等待，消耗 **0.5 RTT**。
	5.  **Server Hello**：
	    *   **动作**：服务器选择 TLS 版本、加密套件，发送自己的证书、另一个随机数（`Server Random`）。
	    *   **时延**：服务器回复到达客户端，又消耗 **0.5 RTT**。总耗时 **1 RTT**。
	6.  **Client Key Exchange**：
	    *   **动作**：客户端验证服务器证书。生成第三个随机数（`Pre-Master Secret`），用服务器证书中的公钥加密后发送给服务器。同时发送 **Change Cipher Spec** 消息，表示后续消息将加密。
	    *   **时延**：客户端发送后等待，消耗 **又一个 0.5 RTT**。总耗时 **1.5 RTT**。
	7.  **Server Finish**：
	    *   **动作**：服务器用私钥解密出 `Pre-Master Secret`。双方根据三个随机数生成相同的`Master Secret`（会话密钥）。服务器发送 **Change Cipher Spec** 和 **Finished** 消息，表示握手完成，后续消息加密。
	    *   **时延**：服务器回复到达客户端，消耗 **最后 0.5 RTT**。总耗时 **2 RTT**。此时安全通道建立，应用数据（如 HTTP 请求）才可以被加密发送。
	
	**优化 (TLS 1.3 和 Session Resumption)**：
	*   **TLS 1.3**：通过优化流程，将握手减少到 **1 RTT**，甚至通过 **0-RTT** 模式实现极致速度（但有重放攻击风险）。
	*   **会话恢复**：如果客户端和服务器之前通信过，可以通过 Session ID 或 Session Ticket 跳过证书验证和密钥交换步骤，在 **1 RTT** 内完成恢复握手。
	
	### 3. TCP 慢启动 (Slow Start)
	
	**目的**：TCP 的一种拥塞控制机制。用于在连接初期或检测到网络拥塞后，**探测当前网络路径的可用带宽**，避免一次性发送过多数据导致网络瘫痪。
	
	**减速过程（核心原理）**：
	慢启动的核心是一个 **拥塞窗口（Congestion Window, cwnd）**，它限制了发送方在收到一个ACK之前所能发送的最大数据量（MSS）。
	
	1.  **初始状态**：连接刚建立时，`cwnd` 很小（通常为 2-10 个 MSS）。
	2.  **指数增长**：发送方每收到一个 ACK，`cwnd` 就增加 1 个 MSS。这导致**每个 RTT 内，`cwnd` 都会翻倍**。
	    *   `RTT 1`: 发送 `cwnd = 2` 个包 -> 收到 2 个 ACK -> `cwnd = 2 + 2 = 4`
	    *   `RTT 2`: 发送 `cwnd = 4` 个包 -> 收到 4 个 ACK -> `cwnd = 4 + 4 = 8`
	    *   `RTT 3`: 发送 `cwnd = 8` 个包 -> 收到 8 个 ACK -> `cwnd = 8 + 8 = 16`
	    *   ... 以此类推。
	3.  **增长终止与“减速”**：这种指数增长不会一直持续下去，它会在以下情况下终止：
	    *   **到达慢启动阈值（ssthresh）**：当 `cwnd` 增长到一个预设的阈值时，会进入**拥塞避免（Congestion Avoidance）** 阶段，增长从指数变为线性（每个 RTT 只增加 1 MSS），变得非常“慢”。
	    *   **检测到包丢失（实际减速）**：这是**最主要的减速触发条件**。如果发送方检测到数据包丢失（超时未收到ACK或收到重复的ACK），它会认为网络发生了拥塞。
	        *   **行为**：立即将 `ssthresh` 设置为当前 `cwnd` 的一半（但不小于 2）。
	        *   然后**将 `cwnd` 重置为一个很小的值（通常是 1 MSS）**。
	        *   重新开始**慢启动**过程，直到 `cwnd` 达到新的、更低的 `ssthresh`，再进入线性增长的拥塞避免阶段。
	
	### 总延时影响	建立一个安全的 HTTPS 连接并开始高效传输数据的总延迟非常高：
	
	**HTTPS 首请求总延迟 ≈ TCP握手 + TLS握手 + TCP慢启动**
	                    **≈ 1 RTT + 2 RTT + 数个RTT的慢启动**
	                    **≈ 3+ RTT**
	
	**这意味着，即使服务器处理请求的速度是0毫秒，一个位于美国的用户访问亚洲的服务器，由于物理距离导致的 RTT 可能高达 200-300ms，那么他在看到页面第一个字节之前就已经等待了 600-900ms！**
	
	**优化手段**：
	*   **TCP Fast Open (TFO)**：允许在 TCP 握手的 SYN 包中就携带数据，减少 1 RTT。
	*   **TLS 1.3**：将 TLS 握手从 2 RTT 减少到 1 RTT。
	*   **CDN**：将服务器部署在离用户更近的地方，从根本上降低 RTT。
	*   **HTTP/2**：多路复用允许在同一个连接上并行交错多个请求，减少了建立新连接的开销。
	*   **HTTP/3 (QUIC)**：将 TCP 和 TLS 握手合并，通常只需 1 RTT 甚至 0 RTT 就能建立安全连接，彻底解决队头阻塞问题。
	
- **网络迁移需要重新连接**：“四元组”，IP改变需要重新连接。当移动设备的网络从 4G 切换到 WIFI 时，意味着 IP 地址变化了，那么就必须要断开连接，然后重新建立连接。而建立连接的过程包含 TCP 三次握手和 TLS 四次握手的时延，以及 TCP 慢启动的减速过程，给用户的感觉就是网络突然卡顿了一下，因此连接的迁移成本是很高的。

### HTTP/3做了什么优化？

基于UDP的QUIC协议
- **无队头阻塞**：在同一条连接上并发传输多个Stream。**当某个流存在丢包时，只会阻塞这个流，其他不受影响，不存在队头阻塞问题**
- **更快的连接建立**：HTTP/3 的 QUIC 协议并不是与 TLS 分层，而是 **QUIC 内部包含了 TLS**，它在自己的帧会携带 TLS 里的“记录”，再加上 QUIC 使用的是** TLS/1.3**，因此**仅需 1 个 RTT 就可以「同时」完成建立连接与密钥协商**，在第二次连接的时候，**应用数据包可以和 QUIC 握手信息（连接信息 + TLS 信息）一起发送，达到 0-RTT 的效果**。
- **连接迁移**： 而 QUIC 协议没有用四元组的方式来“绑定”连接，而是**通过连接 ID 来标记通信的两个端点**，客户端和服务器可以各自选择一组 ID 来标记自己，因此即使移动设备的网络变化后，导致 IP 地址变化了，只要仍保有上下文信息（比如连接 ID、TLS 密钥等），就可以“无缝”地复用原连接，消除重连的成本，没有丝毫卡顿感，达到了连接迁移的功能。
- 
- 头部压缩（QPACK算法（静态表，动态表，Huffman编码）：两个特殊单向流来同步双方的动态表（QPACK Encoder Stream将字典传输给对方，QPACK Decoder Stream回应收到字典并更新动态表）

### HTTP和HTTPS的区别有哪些？
- HTTP 是超文本传输协议，信息是明文传输，存在安全风险的问题。 HTTPS 则解决 HTTP 不安全的缺陷，在 TCP 和 HTTP 网络层之间**加入了 SSL/TLS 安全协议**，使得报文能够加密传输。 
- HTTP 连接建立相对简单， TCP 三次握手之后便可进行 HTTP 的报文传输。 而 HTTPS 在 TCP 三次握手之后，**还需进行 SSL/TLS 的握手过程**，才可进入加密报文传输。 
- 两者的默认端口不一样，**HTTP 默认端口号是 80，HTTPS 默认端口号是 443**。 
- HTTPS 协议**需要向 CA（证书权威机构）申请数字证书**，来保证服务器的身份是可信的。

### HTTPS解决了HTTP的哪些问题？如何解决？
窃听风险、篡改风险、冒充风险

1. 混合加密：对称加密+非对称加密
	- **通信建立前采用 非对称加密 的方式交换 会话密钥**，后续不再使用非对称加密。使用**两个密钥（公钥，私钥）**，公钥可以任意分发，私钥保密，解决了密钥交换问题但速度慢
	- **通信过程**全部使用 **对称加密** 的 会话密钥 的方式加密明文数据。**只使用一个密钥**，运算速度快，密钥必须保密，无法做到安全的密钥交换
	  
	- **公钥加密，私钥解密**。这个目的是为了**保证内容传输的安全**，因为被公钥加密的内容，其他人是无法解密的，只有持有私钥的人，才能解密出实际的内容；
	- **私钥加密，公钥解密**。这个目的是为了**保证消息不会被冒充**，因为私钥是不可泄露的，如果公钥能正常解密出私钥加密的内容，就能证明这个消息是来源于持有私钥身份的人发送的。**能用Alice的公钥成功验证的签名，一定是Alice用她的私钥生成的，从而证明了消息来源于Alice。**
2. 摘要算法+数字签名
	- 哈希值是唯一的，且无法通过哈希值推导出内容。通过哈希算法可以确保内容不会被篡改，但是并不能保证「内容 + 哈希值」不会被中间人替换，因为这里缺少对客户端收到的消息是否来源于服务端的证明。![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809181901063.png)
	- 解决：对内容的哈希值加密。私钥由服务端保管，然后服务端向客户端颁发对应的公钥。![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809182252813.png)
3. 数字证书
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250809183134823.png)

### HTTPS如何优化？

- 性能损耗
	- TLS协议握手过程
	- 握手后的加密报文传输
- 优化
	- 硬件优化：HTTPS协议属于计算密集型（性能瓶颈在CPU的计算能力）选择支持AES-NI特性的CPU（优化了AES算法）
	- 软件优化
		- 软件升级
		- 协议优化：ECDHE算法+ x25519椭圆曲线
	- TLS升级：TLS/1.3把Hello和公钥交换合并成一条消息，只需要1RTT就能完成握手
	- 证书升级
		- 证书传输：使用椭圆曲线ECDSA证书，密钥长度比RSA短
		- 证书验证：OCSP Stapling：周期性向CA查询证书的状态，获得一个带有时间戳和签名的响应结果并缓存，这样TLS握手的时候不需要再访问CA服务器，减少了网络通信的开销，提高了证书校验的效率
	- **会话复用**（TLS Session Resumption）
		- session ID：客户端和服务端在第一次握手后会缓存会话密钥，用唯一的Session ID来标识。第二次访问时客户端的Hello信息会带上Session ID，服务端据此查找，只用一次消息往返就可以进行安全通信。内存中的会话密钥会定期失效
			- 缺点：服务器需要保存所有客户端的会话密钥，内存压力大。网站多使用多台服务器通过负载均衡提供服务，客户端访问不一定命中上一次的服务器，需要重新握手建立通信
		- Session Ticket：第一次通信时服务端会把会话密钥作为Ticket发送给客户端，进行缓存。再次连接时客户端会发生Ticket，服务端解密后获取上一次的会话密钥，并验证有效期
		- Pre-shared Key（TLS/1.3），只需0RTT就可以恢复会话。将Ticket和HTTP一起发送
		- 缺陷：无前向安全性，重放攻击


### HTTPS是如何建立连接的？其间交互了什么？

1. TLS握手（四次通信）
	1. 使用 **RSA 密钥协商算法**的最大问题是**不支持前向保密**。 因为客户端传递随机数（用于生成对称加密密钥的条件之一）给服务端时使用的是公钥加密的，服务端收到后，会用私钥解密得到随机数。所以一旦服务端的私钥泄漏了，过去被第三方截获的所有 TLS 通讯密文都会被破解。
		![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250810141304604.png)
		![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250810141816750.png)
	2. ECDHE算法
		1. 客户端：【Client Hello】：TLS版本号，随机数（Client Random），密码套件列表
		2. 服务端：【Server Hello】：验证版本号，生成随机数（Server Random），选取密码套件。【Certificate】：数字证书。【Server Key Exchange】：选择椭圆曲线，生成随机数作为服务端椭圆曲线的私钥，根据椭圆基点G和私钥计算出服务端的椭圆曲线公钥。【Server Hello Done】
		3. 客户端：逐级校验证书，用证书公钥验证签名。生成随机数作为客户端椭圆曲线的私钥，和基点G计算出客户端的椭圆曲线公钥。【Client Key Exchange】：客户端椭圆曲线公钥。【Change Cipher Spec】：最终会话密钥（Client Random+Server Random+ x（根据ECDHE算法计算出的共享密钥）。【Encrypted Handshake Message】
		4. 服务端：【Change Cipher Spec】和【Encrypted Handshake Message】
		5. 在第四次握手前发送加密的数据
		6. ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250811123538942.png)


	3. TLS1.3（两次通信 1RTT）（只支持ECDHE算法）
		1. 客户端：【Client Hello】：支持的椭圆曲线以及它们对应的公钥，TLS版本号，随机数（Client Random），密码套件列表
		2. 服务端：选定一个椭圆曲线等参数，返回服务端公钥
		3. 客户端：计算会话密钥，进行应用程序的加密传输

### 数字证书签发和验证流程
CA 签发证书的过程，如左边部分：
- 首先 CA 会把持有者的公钥、用途、颁发者、有效时间等信息打成一个包，然后对这些信息进行 Hash 计算，得到一个 Hash 值； 
- 然后 CA 会使用自己的私钥将该 Hash 值加密，生成 Certificate Signature，也就是 CA 对证书做了签名； 
- 最后将 Certificate Signature 添加在文件证书上，形成数字证书；
客户端校验服务端的数字证书的过程，如右边部分： 
- 首先客户端会使用同样的 Hash 算法获取该证书的 Hash 值 H1； 
- 通常浏览器和操作系统中集成了 CA 的公钥信息，浏览器收到证书后可以使用 CA 的公钥解密 Certificate Signature 内容，得到一个 Hash 值 H2 ； 
- 最后比较 H1 和 H2，如果值相同，则为可信赖的证书，否则则认为证书不可信。
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250810143606459.png)
RSA 和 ECDHE 握手过程的区别： RSA 密钥协商算法「不支持」前向保密，ECDHE 密钥协商算法「支持」前向保密； 使用了 RSA 密钥协商算法，TLS 完成四次握手后，才能进行应用数据传输，而对于 ECDHE 算法，客户端可以不用等服务端的最后一次 TLS 握手，就可以提前发出加密的 HTTP 数据，节省了一个消息的往返时间（这个是 RFC 文档规定的，具体原因文档没有说明，所以这点我也不太明白）； 使用 ECDHE， 在 TLS 第 2 次握手中，会出现服务器端发出的「Server Key Exchange」消息，而 RSA 握手过程没有该消息；



### HTTPS一定安全可靠吗？

### 如何避免被中间人抓取数据？

保证电脑的安全，不被病毒入侵，不点击同意非法证书。还可以通过HTTPS双向认证来避免


### 既然有HTTP为什么还要有RPC
- 纯裸 TCP 是能收发数据，但它是个无边界的数据流，上层需要定义消息格式用于定义消息边界。于是就有了各种协议，HTTP 和各类 RPC 协议就是在 TCP 之上定义的应用层协议。 
- RPC 本质上不算是协议，而是一种调用方式，而像 gRPC 和 Thrift 这样的具体实现，才是协议，它们是实现了 RPC 调用的协议。目的是希望程序员能像调用本地方法那样去调用远端的服务方法。
- 同时 RPC 有很多种实现方式，不一定非得基于 TCP 协议。 从发展历史来说，HTTP 主要用于 B/S 架构，而 RPC 更多用于 C/S 架构。但现在其实已经没分那么清了，B/S 和 C/S 在慢慢融合。很多软件同时支持多端，所以对外一般用 HTTP 协议，而内部集群的微服务之间则采用 RPC 协议进行通讯。 
- RPC 其实比 HTTP 出现的要早，且比目前主流的 HTTP/1.1 性能要更好，所以大部分公司内部都还在使用 RPC。 
- HTTP/2.0 在 HTTP/1.1 的基础上做了优化，性能可能比很多 RPC 协议都要好，但由于是这几年才出来的，所以也不太可能取代掉 RPC

### 既然有HTTP为什么要由WebSocket
- TCP 协议本身是全双工的，但我们最常用的 HTTP/1.1，虽然是基于 TCP 的协议，但它是半双工的，对于大部分需要服务器主动推送数据到客户端的场景，都不太友好，因此我们需要使用支持全双工的 WebSocket 协议。 
- 在 HTTP/1.1 里，只要客户端不问，服务端就不答。基于这样的特点，对于登录页面这样的简单场景，可以使用定时轮询或者长轮询的方式实现服务器推送(comet)的效果。 
- 对于客户端和服务端之间需要频繁交互的复杂场景，比如网页游戏，都可以考虑使用 WebSocket 协议。 
- WebSocket 和 socket 几乎没有任何关系，只是叫法相似。 
- 正因为各个浏览器都支持 HTTP协 议，所以 WebSocket 会先利用HTTP协议加上一些特殊的 header 头进行握手升级操作，升级成功后就跟 HTTP 没有任何关系了，之后就用 WebSocket 的数据格式进行收发数据。
- 建立WebSocket连接
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250811145349244.png)

- 消息格式
	- 数据格式：数据头（内含payload长度）+payload data
	- opcode字段：数据帧类型
	- payload：传输的数据的长度，单位为字节。最开始的7bit位payload的值为 0~125
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250811144945806.png)


TCP 粘包
TCP keepalive VS HTTP Keep-Alive

## **计算密集型**
（Compute-Intensive）是指任务或应用程序的主要性能瓶颈在于**CPU的计算能力**，而非其他资源（如内存带宽、磁盘I/O或网络延迟）。这类任务需要大量的数学运算、逻辑处理或复杂算法，通常表现为：

### 核心特征：
1. **高CPU占用率**：任务运行时CPU利用率接近100%，需要长时间进行数值计算（如科学模拟、加密解密、图像渲染等）。
2. **低I/O需求**：计算过程中很少读写数据或与外部系统交互。
3. **并行化潜力**：许多计算密集型任务可以分解为多个子任务并行处理（例如矩阵运算、机器学习训练）。

### 典型场景：
- **科学计算**：气候建模、流体动力学模拟、量子化学计算。
- **密码学**：加密/解密、哈希碰撞计算。
- **图形渲染**：3D建模、视频编码/解码。
- **机器学习**：深度学习模型训练、大规模数据分析。
- **区块链**：挖矿中的哈希运算（如PoW机制）。

### 优化方向：
1. **硬件**：使用多核CPU、GPU（并行计算）或专用加速器（如TPU、FPGA）。
2. **算法**：优化计算复杂度（例如将O(n²)算法优化为O(n log n)）。
3. **并行编程**：利用多线程（如OpenMP）、分布式计算（如MPI）或框架（如CUDA for GPU）。

### 对比其他类型：
- **I/O密集型**：性能受磁盘/网络速度限制（如数据库查询）。
- **内存密集型**：需要大容量或高速内存（如大数据处理）。

### 示例代码（Python中计算斐波那契数列）：
```python
def fib(n):  # 计算密集型函数
    if n <= 1:
        return n
    return fib(n-1) + fib(n-2)

print(fib(35))  # 大量递归计算
```

若任务长时间占用CPU且无明显等待，即可归类为计算密集型。


## **前向安全性**
（Forward Secrecy，简称 **FS**，也称为 **Perfect Forward Secrecy, PFS**）是一种加密通信的安全特性，确保即使长期密钥（如服务器的私钥）在未来被泄露，**过去的通信记录也无法被解密**。  

---

### **核心原理**
1. **会话密钥临时生成**  
   - 每次会话（如 HTTPS 连接）使用**临时密钥**（Ephemeral Key）进行加密，会话结束后立即销毁。  
   - 即使攻击者获取了服务器的长期私钥，也无法解密之前的通信内容。  

2. **依赖迪菲-赫尔曼（Diffie-Hellman, DH）密钥交换**  
   - 前向安全性通常通过 **ECDHE（Elliptic Curve Diffie-Hellman Ephemeral）** 或 **DHE（Diffie-Hellman Ephemeral）** 实现。  
   - 这些算法确保每次会话的密钥独立生成，不依赖长期密钥。  

---

### **为什么前向安全性重要？**
✅ **防止历史数据被解密**  
   - 如果服务器私钥泄露（如黑客入侵、法律强制要求），没有前向安全性的通信可能被批量解密。  
   - 例如，2014年 **Heartbleed** 漏洞导致大量服务器的私钥泄露，但采用 PFS 的网站（如 Google、Cloudflare）未受影响。  

✅ **符合现代安全标准**  
   - TLS 1.3 **强制要求** 前向安全性，淘汰了不支持的加密套件（如 RSA 密钥交换）。  
   - PCI DSS、GDPR 等合规标准推荐使用 PFS。  

---

### **如何实现前向安全性？**
#### **1. 在 TLS/SSL 中**
- **支持的密钥交换算法**：  
  - `ECDHE_RSA`（推荐，结合 ECDH 和 RSA 签名）  
  - `ECDHE_ECDSA`（更高效，使用 ECC 证书）  
  - `DHE_RSA`（传统 DH，性能较差）  

- **不支持的（无前向安全性）**：  
  - `RSA` 密钥交换（密钥直接由服务器 RSA 私钥加密，泄露后历史通信可解密）  

#### **2. 配置示例（Nginx/Apache）**
```nginx
ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256';
ssl_prefer_server_ciphers on;
```

#### **3. 检查网站是否支持 PFS**
- 使用 **Qualys SSL Labs** 测试：  
  [https://www.ssllabs.com/ssltest/](https://www.ssllabs.com/ssltest/)  
  - 如果密钥交换是 `ECDHE` 或 `DHE`，则支持 PFS。  

---

### **前向安全性的局限性**
⚠ **不保护未来的通信**  
   - 如果攻击者**实时监听了通信**（如中间人攻击），仍然可以解密数据。  

⚠ **性能开销**  
   - ECDHE/DHE 比 RSA 密钥交换计算量更大（但现代 CPU 如支持 AES-NI 可缓解）。  

---

### **总结**
| **特性**          | **有前向安全性（PFS）** | **无前向安全性** |
|-------------------|----------------------|----------------|
| **密钥交换**       | ECDHE / DHE          | RSA            |
| **历史数据安全**   | ✅ 即使私钥泄露也无法解密 | ❌ 私钥泄露可解密所有记录 |
| **性能**          | 稍慢（但可优化）       | 更快           |
| **TLS 1.3**       | 强制要求              | 不支持         |

**最佳实践**：在 HTTPS、VPN、SSH 等场景中优先启用前向安全性，选择 `ECDHE` 密钥交换算法。



## TCP

- 目录
	- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250812101506098.png)

### 什么是TCP？
- TCP是面向连接的，可靠的，基于字节流的传输层通信协议，是一个工作在传输层的可靠的数据传输服务，能确保接收端收到的数据无间隔，无损坏，无冗余，按序。
	- 面向连接：TCP是一对一连接，不像UDP协议能实现一对多连接
	- 可靠：TCP能够保证不管网络链路如何变化都能将一个报文传输给接收端
	- 基于字节流：消息会被操作系统分组成多个TCP报文，TCP报文是有序的，即使已经收到后面序号的报文，当前序号的报文未收到的情况下，应用层依旧无法处理数据，同时会对重复的报文进行丢弃
### 什么是TCP连接？

TCP连接是 包括socket，序列号，窗口大小的 用来保证可靠性和流量控制维护的 状态信息组合
	socket：包含IP地址和端口号
	序列号：解决乱序问题
	窗口大小：用来做流量控制

### 如何唯一确定一个TCP连接？

TCP的“四元组”可以唯一确定一个TCP连接，源IP，源端口，目标IP，目标端口
源IP和目标IP的字段（32位）在IP头部，通过IP协议发送报文发送给接收端
源端口和目标端口的字段（16位）在TCP头部，告诉TCP协议应该把报文发送给哪个进程

最大TCP连接数 = 客户端IP数 * 客户端端口数
服务端最大并发TCP连接数不能达到最大值，受到文件描述符限制和内存限制（内存被占满之后会发生OOM：**Out Of Memory"（内存不足）**）

### TCP头格式
- 序列号：在计算机建立连接时生成一个随机数作为初始值，通过SYN包发送给接收端主机，每发送一次，就累计一次该数据字节数的大小。用来解决网络包乱序的问题
- 确认序列号：下一次期望收到数据的序号，接收端收到这个序号之后认为序号之前的数据都已经接收到了，解决了丢包问题。
- 控制位
	- ACK：在最初的 SYN 包（建立连接的第一次握手）中，ACK = 0（因为还没有需要确认的数据）。之后的任何报文（包括 SYN-ACK、数据包、FIN 等），ACK 必须为 1（表示该报文携带确认信息）
	- RST：为1时表示TCP连接异常必须强制断开连接
	- SYN：1：希望建立连接，并在“序列号”字段进行序列号初始值的设定
	- FIN：1：希望断开连接。通信双方可以交换FIN为1的TCP段
- ![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250812101712869.png)

### TCP三次握手

- 客户端和服务端都处于closed状态。服务端主动监听某个端口，处于listen状态
1. 客户端随机初始化序号（client-isn），将序号置于TCP首部的“序号”字段中；
	把SYN标志位 置为1，表示SYN报文。
	将第一个SYN报文发送给服务端，表示发起连接，报文不含应用层数据，
	之后处于SYN-SENT状态。
2. 服务端收到SYN后，随机初始化序号（server-isn），填入TCP首部的“序号”字段中。
	把TCP首部的 确认应答号 填入 client-isn + 1，
	把ACK和SYN标志位 置为1。
	把报文发送给客户端，报文不包含应用层数据，
	处于SYN-RCVD状态
3. 客户端收到ACK和SYN后，把ACK标志位 置为1；
	在“确认应答号”填入 server-isn + 1；
	把ACK应答报文发送给服务端，可以携带客户到服务端的数据
	进入ESTABLISHED状态
- 服务端收到ACK报文后，处于ESTABLISHED状态
- 连接建立完成，可以相互发送数据
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250801224949041.png)

### 为什么是三次握手？不是两次、四次？
为什么三次握手才可以初始化socket，序列号和窗口大小并建立连接？

- 阻止重复历史连接的初始化
	- 场景：客户端发送SYN（seq = 90）的报文，之后宕机了，同时网络发生阻塞；客户端重启后发生新的SYN（seq = 100）的报文。（不是重传，重传的SYN的序列号是一样的）
	- 网络拥堵的情况下：
		- 旧SYN先抵达，服务端返回一个SYN+ACK，确认号为（90+1）；
		- 客户端收到后联系上下文比对，发现自己想要收到的确认号是101（100+1），于是发送RST终止连接
		- 服务端收到RST后释放连接
		- 新的SYN报文到达，完成三次握手
	- 服务端在收到RST之前先收到了新的SYN：
		- 发送Challenge ACK报文给客户端，其中的确认号为之前的确认号91（90+1）
		- 客户端收到之后会返回RST
	- 
	- 两次握手无法阻止重复历史连接
		- 服务端在接收到SYN时发送ACK和SYN，就进入ESTABLISHED状态，可以向对方发送数据，但是客户端未进入ESTABLISHED状态，
		- 如果服务端接受到的是历史连接，客户端就会发送RST给服务端释放连接
		- 之后再次握手建立连接
		- 服务端建立了历史连接，白白发送了数据，浪费了服务端资源
	- 
	- 第三次握手时如果ACK丢失，服务端还能接受数据建立连接吗
		- 能，因为数据报文中有ACK标志位和确认号
	  
- 同步双方的序列号
	- 序列号的作用
		- 接收方可以去除重复数据
		- 接收方可以根据序列号按序接收数据
		- 可以标识发送的报文对方是否已经接收
	- 只有两次握手没办法确保两方都收到了序列号
	
- 避免浪费资源
	- 只有两次握手，网络阻塞
		- 客户端重复发送SYN，服务端接收到SYN后都建立连接，发送数据，重复建立连接重复发送数据，浪费服务端资源

### 为什么初始化序列号要求不一样

- 防止历史报文被下一个相同的四元组连接接收
- 为了安全性，防止黑客伪造相同序列号的TCP报文被对方接收

### 多个TCP连接可以绑定同一个端口吗？

- 如果两个TCP绑定的是同一个IP地址和同一个端口，则在执行bind（）时会出错，会出现“Address already in use”。注意：如果其中一个端口绑定的是0.0.0.0地址，相当于把主机上的所有地址都绑定了，也会发出错误信息。
- 避免0.0.0.0地址的影响：对socket设置SO_REUSEADDR
- 多个进程绑定相同的IP和端口：对socket设置SO_REUSESPORT

### 重启TCP服务进程时为什么会出现“Address already in use”错误信息

重启时表示通过服务端主动关闭进程，进行四次挥手，对于主动关闭方会有一段TIME_WAIT状态，大约2MSL，TIME_WAIT状态连接的IP和port还是原来的，被认为是有效组合，此时还保留原来的IP地址和端口，相同机器不能进行绑定，执行bind（）函数时报错。等TIME_WAIT状态连接结束后，就能够成功重启
- 避免，在执行bind（）函数之前，对socket设置SO_REUSEADDR

### 客户端端口可以重复使用吗？
TCP连接使由四元组唯一确定的，四元组中其中一个元素不同，则代表不同TCP连接

### 客户端TCP连接的TIME_WAIT状态过多，会导致端口资源耗尽无法连接吗？
- 要看是否连接在同一个服务器上，
- 要连接在同一个服务器上：打开 net.ipv4.tcp_tw_reuse 这个内核参数。 因为开启了这个内核参数后，客户端调用 connect 函数时，如果选择到的端口，已经被相同四元组的连接占用的时候，就会判断该连接是否处于 TIME_WAIT 状态。 如果该连接处于 TIME_WAIT 状态并且 TIME_WAIT 状态持续的时间超过了 1 秒，那么就会重用这个连接，然后就可以正常使用该端口了。

### connect函数选择端口的过程
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250812162525831.png)


### UDP头格式

UDP不提供复杂的控制机制，利用IP提供面向无连接的通信服务，头部只有8字节（64位）
目标和源端口：告诉UDP协议把报文发送给哪个进程
包长度：UDP首部长度和数据长度之和
校验和：为了提供可靠的UDP首部和数据而设计，防止收到在网络传输中损坏的UDP包
![](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250812114032850.png)

### TCP和UDP的区别

| **对比维度**  | **TCP**                  | **UDP**              |
| --------- | ------------------------ | -------------------- |
| **连接方式**  | 面向连接（三次握手）               | 无连接，即刻传输数据           |
| **可靠性**   | 可靠（不丢包、不乱序）              | 不可靠（可能丢包、乱序）         |
| **首部开销**  | 长度较长，随“选项”改变             | 长度固定，开销较小            |
| **速度**    | 较慢（额外控制开销）               | 更快（轻量级）              |
| **数据边界**  | 字节流（无边界）                 | 数据包（有边界）             |
| **数据顺序**  | 保证按序到达                   | 可能乱序                 |
| **流量控制**  | 通过滑动窗口调节                 | 无                    |
| **拥塞控制**  | 有（如慢启动、拥塞避免）             | 无                    |
| **多播/广播** | 不支持                      | 支持                   |
| **服务对象**  | 一对一                      | 支持一对一，一对多，多对多的交互通信   |
| **传输方式**  | 流式传输，没有边界，有序可靠           | 一个包一个包发送，有边界，可能会丢包乱序 |
| **分片不同**  | 大于MSS在传输层分片，在传输层组装，丢失重传  | 大于MTU在IP层分片，在IP层组装   |
| **适用场景**  | FTP文件传输、网页浏览（HTTP，HTTPS） | 视频会议、在线游戏            |
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250812151636754.png)
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250812161525455.png)
![image.png](https://kmk1132-obs-1370539359.cos.ap-guangzhou.myqcloud.com/20250812161700483.png)



### TCP和UDP可以连接到同一个端口吗？

可以。因为在内核中TCP和UDP是不同模块。UDP和TCP传输协议在内核中是由两个完全独立的软件模块实现的。主机收到数据包之后，IP包头的协议号字段知道数据包是TCP的还是UDP的，可以据此发送给对应模块处理，送给TCP/UDP的报文根据端口号确定送给哪个应用进程进行处理